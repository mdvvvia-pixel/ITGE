# Уточненные требования проекта Table-Graph Editor

**Дата обновления:** 2026-01-06  
**Статус:** Уточнено, готово к разработке MVP

---

## ✅ Полученные ответы на критические вопросы

### 1. Интерпретация матрицы для графиков

**Ответ получен:** ✅

#### Режим "По столбцам":
- **Столбец 1** = координаты X для всех кривых
- **Столбцы 2, 3, 4, ...** = значения Y (каждый столбец = отдельная кривая)
- **Первая строка** = метки/индексы (не участвует в числовых данных графика)

**Пример:**
```
Матрица 5×4 (5 строк, 4 столбца):
     Col1  Col2  Col3  Col4
Row1  "A"   0.1   0.2   0.3    <- метки (не используются в графике)
Row2   1.0   2.0   3.0   4.0
Row3   2.0   4.0   6.0   8.0
Row4   3.0   6.0   9.0  12.0
Row5   4.0   8.0  12.0  16.0

График будет:
- X: [1.0, 2.0, 3.0, 4.0] (из столбца 1, строки 2-5)
- Кривая 1: Y = [2.0, 4.0, 6.0, 8.0] (столбец 2)
- Кривая 2: Y = [3.0, 6.0, 9.0, 12.0] (столбец 3)
- Кривая 3: Y = [4.0, 8.0, 12.0, 16.0] (столбец 4)
```

#### Режим "По строкам":
- **Строка 1** = координаты X для всех кривых
- **Строки 2, 3, 4, ...** = значения Y (каждая строка = отдельная кривая)
- **Первый столбец** = метки/индексы (не участвует в числовых данных графика)

**Пример:**
```
Та же матрица 5×4:
     Col1  Col2  Col3  Col4
Row1  "A"   0.1   0.2   0.3    <- X координаты (строки 2-5 используют эти значения)
Row2   1.0   2.0   3.0   4.0   <- Кривая 1: Y = [2.0, 3.0, 4.0]
Row3   2.0   4.0   6.0   8.0   <- Кривая 2: Y = [4.0, 6.0, 8.0]
Row4   3.0   6.0   9.0  12.0   <- Кривая 3: Y = [6.0, 9.0, 12.0]
Row5   4.0   8.0  12.0  16.0   <- Кривая 4: Y = [8.0, 12.0, 16.0]

График будет:
- X: [0.1, 0.2, 0.3] (из строки 1, столбцы 2-4)
- Кривая 1: Y = [2.0, 3.0, 4.0] (строка 2, столбцы 2-4)
- Кривая 2: Y = [4.0, 6.0, 8.0] (строка 3)
- Кривая 3: Y = [6.0, 9.0, 12.0] (строка 4)
- Кривая 4: Y = [8.0, 12.0, 16.0] (строка 5)
```

#### Выбор части данных для графика:
- ✅ Пользователь может выделить несколько столбцов/строк в таблице
- ✅ График строится только для выделенных столбцов/строк
- ✅ **Уточнено:** Выделение в uitable (клик + Shift/Ctrl для множественного выбора)
- ✅ Использовать `uitable.Selection` для определения выделенных столбцов/строк

---

### 2. Добавление/удаление точек

**Ответ получен:** ✅
- ⏸️ **Отложено на последующие этапы разработки**
- Не входит в MVP

---

### 3. Сохранение в workspace

**Ответ получен:** ✅
- ✅ **Перезаписывать исходную переменную**
- ✅ **Всегда спрашивать подтверждение** перед перезаписью (использовать `uiconfirm`)

---

### 4. Зависимые столбцы

**Ответ получен:** ✅
- ⏸️ **Отложено на последующие этапы разработки**
- Не входит в MVP

---

## ✅ Дополнительные вопросы - РЕШЕНЫ

### Вопрос 1: Выбор столбцов/строк для графика

**✅ ОТВЕТ ПОЛУЧЕН:**
- ✅ Выделение в uitable (клик + Shift/Ctrl для множественного выбора)
- ✅ Можно выбрать несколько несмежных столбцов/строк
- ✅ Использовать `uitable.Selection` для определения выделенных элементов
- ⚠️ **Требует уточнения при реализации:** Что делать, если выделен только один столбец/строка? (нужна ли X координата?)
- ⚠️ **Требует уточнения при реализации:** Сохраняется ли выбор при переключении режима "по столбцам" ↔ "по строкам"?

---

### Вопрос 2: Обработка меток/индексов

**✅ ОТВЕТ ПОЛУЧЕН:**
- ✅ Числовые данные в метках используются как метки (не как данные для графика)
- ✅ Метки отображаются в легенде графика
- ✅ Метки не редактируются (read-only в таблице)
- ✅ Первая строка/столбец всегда интерпретируются как метки

---

### Вопрос 3: Минимальные требования к данным

**⚠️ Требует уточнения при реализации:**
- [ ] Минимальный размер матрицы для режима "по столбцам"?
  - Минимум: 2 строки (1 метка + 1 точка данных) × 2 столбца (1 X + 1 Y)?
  - Или минимум: 3 строки × 2 столбца (1 метка + 2 точки данных)?
- [ ] Минимальный размер матрицы для режима "по строкам"?
- [ ] Что делать, если данных недостаточно? (показать ошибку, предупреждение?)

**Рекомендация:** Минимум 2 строки × 2 столбца (1 метка + минимум 1 точка данных)

---

### Вопрос 4: Сохранение меток

**⚠️ Требует уточнения при реализации:**
- [ ] Сохраняются ли метки вместе с данными?
- [ ] Если метки текстовые, а матрица числовая, как это обработать?
  - [ ] Создать отдельную переменную для меток?
  - [ ] Использовать table вместо matrix?
  - [ ] Игнорировать метки при сохранении?

**Рекомендация:** Сохранять метки вместе с данными в исходной структуре (если метки текстовые, возможно использовать table)

---

## 📋 Обновленный MVP функционал

### Обязательно для MVP:
- ✅ Загрузка числовой матрицы из workspace
- ✅ Отображение в таблице (с поддержкой меток)
- ✅ Построение графика:
  - ✅ Режим "по столбцам"
  - ✅ Режим "по строкам"
  - ✅ Выбор части данных для графика
- ✅ Перетаскивание точек (режим XY)
- ✅ Обновление таблицы при изменении графика
- ✅ Обновление графика при изменении таблицы
- ✅ Кнопка сохранения в workspace (перезапись исходной переменной)

### Отложено на последующие этапы:
- ⏸️ Добавление/удаление точек
- ⏸️ Зависимые столбцы
- ⏸️ Режимы редактирования X/Y (только XY для MVP)
- ⏸️ Undo/Redo
- ⏸️ Настройки отображения (базовые настройки можно оставить)

---

## 🎯 Рекомендации по реализации

### 1. Структура данных

```matlab
properties (Access = private)
    originalData        % Исходные данные (полная матрица)
    currentData         % Текущие редактируемые данные
    selectedVariable    % Выбранная переменная из workspace
    editMode = 'XY'     % Режим редактирования: 'XY' (для MVP)
    currentPlotType = 'columns' % 'columns' или 'rows'
    
    % Для выбора части данных
    selectedColumns = [] % Индексы выбранных столбцов (для режима "по столбцам")
    selectedRows = []    % Индексы выбранных строк (для режима "по строкам")
    
    % Метки
    rowLabels = {}      % Метки строк (первый столбец в режиме "по строкам")
    columnLabels = {}   % Метки столбцов (первая строка в режиме "по столбцам")
end
```

### 2. Алгоритм построения графика

#### Режим "по столбцам":
```matlab
function plotByColumns(app)
    % 1. Извлечь метки (первая строка, столбцы 2:end)
    app.columnLabels = app.currentData(1, 2:end);
    
    % 2. Извлечь X координаты (столбец 1, строки 2:end)
    xData = app.currentData(2:end, 1);
    
    % 3. Если выбраны столбцы, использовать только их
    if isempty(app.selectedColumns)
        yColumns = 2:size(app.currentData, 2);
    else
        yColumns = app.selectedColumns;
    end
    
    % 4. Построить кривые
    for col = yColumns
        yData = app.currentData(2:end, col);
        plot(app.axPlot, xData, yData, '-o', 'DisplayName', app.columnLabels{col-1});
    end
end
```

#### Режим "по строкам":
```matlab
function plotByRows(app)
    % 1. Извлечь метки (первый столбец, строки 2:end)
    app.rowLabels = app.currentData(2:end, 1);
    
    % 2. Извлечь X координаты (строка 1, столбцы 2:end)
    xData = app.currentData(1, 2:end);
    
    % 3. Если выбраны строки, использовать только их
    if isempty(app.selectedRows)
        yRows = 2:size(app.currentData, 1);
    else
        yRows = app.selectedRows;
    end
    
    % 4. Построить кривые
    for row = yRows
        yData = app.currentData(row, 2:end);
        plot(app.axPlot, xData, yData, '-o', 'DisplayName', app.rowLabels{row-1});
    end
end
```

### 3. Обработка меток

**✅ Уточнено:**
- Метки (первая строка/столбец) всегда интерпретируются как метки, даже если числовые
- Метки отображаются в легенде графика
- Метки не редактируются (read-only в таблице)
- Хранить метки вместе с данными в исходной структуре

**Реализация:**
- Первая строка/столбец всегда извлекаются как метки
- Метки используются для `DisplayName` в `plot()` (легенда)
- Ячейки с метками в uitable должны быть нередактируемыми или с валидацией

---

## ✅ Готовность к разработке

### ✅ Все критические вопросы решены:
- ✅ Интерпретация матрицы для графиков
- ✅ Режимы "по столбцам" и "по строкам"
- ✅ Выбор столбцов/строк (выделение в uitable)
- ✅ Обработка меток (числовые как метки, отображение в легенде, read-only)
- ✅ Сохранение (перезапись с подтверждением)
- ✅ Приоритеты функционала (MVP vs будущие этапы)

### ⚠️ Второстепенные вопросы (можно решить при реализации):
- Минимальные требования к данным
- Сохранение меток (структура данных)

### ✅ Статус: **ПОЛНОСТЬЮ ГОТОВО К НАЧАЛУ РАЗРАБОТКИ MVP**

---

## 📝 Следующие шаги

1. **Уточнить детали выбора столбцов/строк** (можно начать без этого)
2. **Создать структуру проекта**
3. **Начать разработку MVP:**
   - Базовая структура App Designer
   - Загрузка переменных
   - Отображение таблицы
   - Построение графика (оба режима)
   - Перетаскивание точек
   - Сохранение

---

**Статус:** Готово к началу разработки MVP! 🚀

