# Финальные требования для MVP Table-Graph Editor

**Дата:** 2026-01-06  
**Статус:** ✅ Все требования уточнены, готово к разработке

---

## 📋 Функциональные требования

### 1. Загрузка данных
- ✅ Загрузка числовой матрицы из workspace MATLAB
- ✅ Dropdown для выбора переменной
- ✅ Автоматическое обновление списка переменных при открытии

### 2. Отображение данных
- ✅ Отображение матрицы в uitable
- ✅ Первая строка/столбец = метки (read-only, не редактируются)
- ✅ Остальные ячейки = числовые данные (редактируемые)
- ✅ Поддержка выделения столбцов/строк (клик + Shift/Ctrl)

### 3. Построение графиков

#### Режим "По столбцам":
- ✅ Столбец 1 = координаты X для всех кривых
- ✅ Столбцы 2, 3, 4, ... = значения Y (каждый столбец = отдельная кривая)
- ✅ Первая строка = метки (не участвует в числовых данных)
- ✅ График строится только для выделенных столбцов

#### Режим "По строкам":
- ✅ Строка 1 = координаты X для всех кривых
- ✅ Строки 2, 3, 4, ... = значения Y (каждая строка = отдельная кривая)
- ✅ Первый столбец = метки (не участвует в числовых данных)
- ✅ График строится только для выделенных строк

#### Тип графика:
- ✅ Точечный график с прямыми отрезками (`plot(..., '-o')`)
- ✅ Несколько кривых на одном графике
- ✅ Легенда с метками из первой строки/столбца

### 4. Редактирование данных

#### Перетаскивание точек:
- ✅ Режим редактирования: только XY (для MVP)
- ✅ Перетаскивание точек мышью на графике
- ✅ Обновление данных в таблице при перетаскивании
- ✅ Соседние точки остаются неподвижными

#### Редактирование в таблице:
- ✅ Прямое редактирование числовых ячеек
- ✅ Обновление графика при изменении таблицы
- ✅ Валидация числовых значений

### 5. Синхронизация
- ✅ Таблица → График: автоматическое обновление при изменении данных
- ✅ График → Таблица: автоматическое обновление при перетаскивании точек
- ✅ Использовать `drawnow limitrate` для оптимизации производительности

### 6. Сохранение
- ✅ Кнопка "Save to Workspace"
- ✅ Перезапись исходной переменной
- ✅ Всегда спрашивать подтверждение перед сохранением (`uiconfirm`)
- ✅ Показывать имя переменной в диалоге подтверждения

---

## 🎨 UI Компоненты

### Обязательные компоненты:
- ✅ `uitable` - таблица данных
- ✅ `uiaxes` - область графика
- ✅ `uidropdown` - выбор переменной из workspace
- ✅ `uidropdown` - выбор типа графика (по столбцам/строкам)
- ✅ `uibutton` - кнопка сохранения
- ✅ `uibuttongroup` с `uiradiobutton` - режим редактирования (XY для MVP)

### Опциональные (можно добавить позже):
- ⏸️ Кнопки Undo/Redo
- ⏸️ Кнопки Add/Delete Point
- ⏸️ Настройки отображения

---

## 🔧 Технические требования

### Структура данных:
```matlab
properties (Access = private)
    originalData        % Исходные данные (полная матрица)
    currentData         % Текущие редактируемые данные
    selectedVariable    % Выбранная переменная из workspace
    editMode = 'XY'     % Режим редактирования: 'XY' (для MVP)
    currentPlotType = 'columns' % 'columns' или 'rows'
    
    % Для выбора части данных
    selectedColumns = [] % Индексы выбранных столбцов
    selectedRows = []    % Индексы выбранных строк
    
    % Метки
    rowLabels = {}      % Метки строк (первый столбец в режиме "по строкам")
    columnLabels = {}   % Метки столбцов (первая строка в режиме "по столбцам")
    
    % Для перетаскивания
    selectedPoint = []  % Индекс выбранной точки [row, col] или [curveIndex, pointIndex]
    isDragging = false  % Флаг перетаскивания
end
```

### Алгоритмы:

#### Построение графика (по столбцам):
```matlab
function plotByColumns(app)
    % 1. Извлечь метки (первая строка, столбцы 2:end)
    app.columnLabels = app.currentData(1, 2:end);
    
    % 2. Извлечь X координаты (столбец 1, строки 2:end)
    xData = app.currentData(2:end, 1);
    
    % 3. Определить выделенные столбцы
    if isempty(app.selectedColumns)
        yColumns = 2:size(app.currentData, 2);
    else
        yColumns = app.selectedColumns;
    end
    
    % 4. Очистить график
    cla(app.axPlot);
    hold(app.axPlot, 'on');
    
    % 5. Построить кривые
    for col = yColumns
        yData = app.currentData(2:end, col);
        label = app.columnLabels{col-1};
        plot(app.axPlot, xData, yData, '-o', 'DisplayName', label);
    end
    
    legend(app.axPlot, 'show');
    hold(app.axPlot, 'off');
end
```

#### Определение выделенных столбцов/строк:
```matlab
function updateSelection(app)
    selection = app.tblData.Selection;
    if isempty(selection)
        app.selectedColumns = [];
        app.selectedRows = [];
        return;
    end
    
    % Определить выделенные столбцы/строки на основе режима
    if strcmp(app.currentPlotType, 'columns')
        app.selectedColumns = unique(selection(:, 2));
        app.selectedColumns = app.selectedColumns(app.selectedColumns > 1); % Исключить столбец X
    else
        app.selectedRows = unique(selection(:, 1));
        app.selectedRows = app.selectedRows(app.selectedRows > 1); % Исключить строку X
    end
end
```

#### Перетаскивание точек:
```matlab
function startDrag(app, src, event)
    % 1. Определить ближайшую точку
    pointIndex = findClosestPoint(app, event.IntersectionPoint);
    app.selectedPoint = pointIndex;
    app.isDragging = true;
    
    % 2. Установить callbacks
    app.UIFigure.WindowButtonMotionFcn = @(src,event) dragPoint(app, event);
    app.UIFigure.WindowButtonUpFcn = @(src,event) stopDrag(app);
end

function dragPoint(app, event)
    if ~app.isDragging, return; end
    
    % 1. Получить новые координаты
    newPoint = event.IntersectionPoint;
    
    % 2. Применить ограничения режима (XY - без ограничений)
    % 3. Обновить данные
    updateDataFromGraph(app, app.selectedPoint, newPoint);
    
    % 4. Обновить график
    updateGraph(app);
    drawnow limitrate;
end

function stopDrag(app)
    app.isDragging = false;
    app.UIFigure.WindowButtonMotionFcn = [];
    app.UIFigure.WindowButtonUpFcn = [];
end
```

---

## ✅ Критерии приемки MVP

### Обязательные:
- [ ] Загрузка переменной из workspace
- [ ] Отображение данных в таблице
- [ ] Построение графика (оба режима)
- [ ] Выделение столбцов/строк в таблице
- [ ] График обновляется при изменении выделения
- [ ] Перетаскивание точек на графике
- [ ] Обновление таблицы при перетаскивании
- [ ] Обновление графика при редактировании таблицы
- [ ] Сохранение с подтверждением
- [ ] Метки отображаются в легенде
- [ ] Метки не редактируются

### Опциональные (можно отложить):
- [ ] Undo/Redo
- [ ] Добавление/удаление точек
- [ ] Режимы редактирования X/Y
- [ ] Настройки отображения
- [ ] Зависимые столбцы

---

## 🚀 План разработки MVP

### Этап 1: Базовая структура (1-2 дня)
1. Создать структуру проекта
2. Создать базовый UI в App Designer
3. Определить properties и структуру данных

### Этап 2: Загрузка и отображение (1-2 дня)
1. Реализовать загрузку переменных из workspace
2. Отображение данных в uitable
3. Обработка меток (read-only ячейки)

### Этап 3: Построение графиков (2-3 дня)
1. Режим "по столбцам"
2. Режим "по строкам"
3. Обработка выделения столбцов/строк
4. Легенда с метками

### Этап 4: Редактирование (2-3 дня)
1. Перетаскивание точек
2. Синхронизация график → таблица
3. Редактирование в таблице
4. Синхронизация таблица → график

### Этап 5: Сохранение и полировка (1-2 дня)
1. Сохранение в workspace
2. Подтверждение перед сохранением
3. Обработка ошибок
4. Тестирование

**Общая оценка:** 7-12 дней разработки

---

## 📝 Заметки для реализации

### Важные детали:
- Метки всегда в первой строке/столбце, даже если числовые
- Выделение в uitable использует `Selection` property
- При перетаскивании использовать `WindowButtonMotionFcn`
- Для производительности использовать `drawnow limitrate`
- Все callback функции должны иметь `try-catch` для обработки ошибок

### Потенциальные проблемы:
- Определение ближайшей точки при клике (учесть масштаб осей)
- Обработка больших матриц (оптимизация обновлений)
- Валидация данных при редактировании таблицы

---

**Статус:** ✅ Готово к началу разработки!

