# Инструкция: Удаление методов перетаскивания из .mlapp

**Дата:** 2026-01-09  
**Статус:** ✅ Требуется выполнить

## 🗑️ Что нужно удалить из .mlapp файла

### Методы для удаления:

1. **dragPoint** - если есть в .mlapp, **УДАЛИТЬ**
2. **stopDrag** - если есть в .mlapp, **УДАЛИТЬ**
3. **checkMouseMovement** - если есть в .mlapp, **УДАЛИТЬ**

### Почему нужно удалить:

Эти функции теперь находятся в отдельных файлах в папке `helpers/`:
- `helpers/dragPoint.m`
- `helpers/stopDrag.m`
- `helpers/checkMouseMovement.m`

Они вызываются как helper функции из callbacks, установленных в `axPlotButtonDown`, и **НЕ должны** быть методами класса в .mlapp файле.

---

## 📝 Инструкция по удалению

### Шаг 1: Откройте .mlapp файл
1. Откройте `TableGraphEditor.mlapp` в App Designer
2. Перейдите в **Code View**

### Шаг 2: Найдите и удалите методы
1. Найдите секцию `methods (Access = private)`
2. Найдите следующие методы (если они есть):
   - `function dragPoint(app, src, event)`
   - `function stopDrag(app, src, event)`
   - `function checkMouseMovement(app, timerObj)`
3. **УДАЛИТЕ** эти методы полностью (включая все их содержимое)

### Шаг 3: Проверьте, что осталось
В .mlapp файле должен остаться **ТОЛЬКО**:
- `function axPlotButtonDown(app, src, event)` - этот метод **НУЖЕН**

### Шаг 4: Проверьте вызовы в axPlotButtonDown
Убедитесь, что в `axPlotButtonDown` вызовы выглядят так:
```matlab
app.UIFigure.WindowButtonMotionFcn = @(src,evt) dragPoint(app, src, evt);
app.UIFigure.WindowButtonUpFcn = @(src,evt) stopDrag(app, src, evt);
```

И таймер:
```matlab
'TimerFcn', @(t,~) checkMouseMovement(app, t)
```

**НЕ должно быть:**
```matlab
app.UIFigure.WindowButtonMotionFcn = @(src,evt) app.dragPoint(src, evt);  ❌
```

### Шаг 5: Сохраните файл
Сохраните `.mlapp` файл

---

## ✅ Что должно остаться в .mlapp

### Методы для перетаскивания:
- ✅ **axPlotButtonDown** - должен быть (строки ~459-716 в METHODS_FOR_MLAPP.m)

### Методы НЕ для перетаскивания (другие функции):
- ✅ startupFcn
- ✅ ddVariableValueChanged
- ✅ ddPlotTypeValueChanged
- ✅ btnSaveButtonPushed
- ✅ tblDataSelectionChanged

---

## 🔍 Как проверить, что все правильно

1. **Проверьте наличие методов:**
   - В Code View найдите `function dragPoint` - **НЕ должно быть**
   - В Code View найдите `function stopDrag` - **НЕ должно быть**
   - В Code View найдите `function checkMouseMovement` - **НЕ должно быть**
   - В Code View найдите `function axPlotButtonDown` - **ДОЛЖНО быть**

2. **Проверьте вызовы в axPlotButtonDown:**
   - Должно быть: `dragPoint(app, src, evt)` (вызов функции)
   - НЕ должно быть: `app.dragPoint(src, evt)` (вызов метода)

3. **Проверьте, что helper функции доступны:**
   - Убедитесь, что папка `helpers/` добавлена в MATLAB path
   - Файлы должны существовать:
     - `helpers/dragPoint.m`
     - `helpers/stopDrag.m`
     - `helpers/checkMouseMovement.m`

---

## ⚠️ Важно

Если методы `dragPoint`, `stopDrag` или `checkMouseMovement` останутся в .mlapp файле:
- ❌ Они будут иметь приоритет над helper функциями из `helpers/`
- ❌ Helper функции из `helpers/` не будут вызываться
- ❌ Перетаскивание может не работать или работать неправильно

**Поэтому их обязательно нужно удалить!**

---

## 📖 Ссылки

- Актуальный код `axPlotButtonDown`: `helpers/METHODS_FOR_MLAPP.m` (строки ~459-716)
- Helper функции:
  - `helpers/dragPoint.m`
  - `helpers/stopDrag.m`
  - `helpers/checkMouseMovement.m`
- Чеклист: `MLAPP_METHODS_CHECKLIST.md`

