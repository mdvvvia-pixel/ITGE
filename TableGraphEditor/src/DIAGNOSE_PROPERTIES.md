# Диагностика проблемы с Properties

**Date:** 2026-01-08  
**Проблема:** Properties добавлены, но не распознаются

---

## 🔍 Шаги диагностики

### Шаг 1: Проверьте синтаксис в Code View

Откройте `.mlapp` в App Designer, Code View и проверьте:

1. **Нет ли красных подчеркиваний?** (синтаксические ошибки)
2. **Правильно ли закрыты секции?** (все `end` на месте)
3. **Нет ли конфликтов имен?** (одинаковые имена свойств)

### Шаг 2: Проверьте структуру

Должно быть так:

```matlab
classdef TableGraphEditor < matlab.apps.AppBase

    properties (Access = public)
        UIFigure  matlab.ui.Figure
        % ... UI компоненты
    end
    
    properties (Access = private)  % ← ЭТА СЕКЦИЯ ДОЛЖНА БЫТЬ
        % === Данные ===
        originalData
        currentData
        % ... остальные свойства
    end
    
    methods (Access = private)
        % методы
    end
    
    methods (Access = public)
        % конструктор и другие методы
    end
    
end
```

### Шаг 3: Запустите проверку

```matlab
app = TableGraphEditor;
checkAppProperties(app);
```

Это покажет, какие свойства найдены.

---

## 🐛 Частые ошибки

### Ошибка 1: Properties добавлены в неправильное место

**Неправильно:**
```matlab
properties (Access = public)
    UIFigure
    originalData  % ← НЕ ЗДЕСЬ!
end
```

**Правильно:**
```matlab
properties (Access = public)
    UIFigure
end

properties (Access = private)  % ← ОТДЕЛЬНАЯ СЕКЦИЯ
    originalData
end
```

### Ошибка 2: Неправильный синтаксис

**Неправильно:**
```matlab
properties (Access = private);
    originalData;
end
```

**Правильно:**
```matlab
properties (Access = private)
    originalData
end
```

### Ошибка 3: Конфликт имен

Если свойство называется так же, как UI компонент, будет конфликт.

---

## ✅ Правильный код для копирования

Скопируйте ЭТОТ код точно (без изменений):

```matlab
properties (Access = private)
    originalData
    currentData
    selectedVariable
    editMode = 'XY'
    currentPlotType = 'columns'
    selectedColumns = []
    selectedRows = []
    rowLabels = {}
    columnLabels = {}
    selectedPoint = []
    isDragging = false
    dragStartPosition = []
    isUpdating = false
end
```

---

## 🔧 Если ничего не помогает

1. **Сохраните резервную копию** `.mlapp` файла
2. **Удалите секцию Properties** полностью
3. **Сохраните файл**
4. **Закройте App Designer**
5. **Откройте заново**
6. **Добавьте Properties заново** точно из `PROPERTIES_TO_ADD.m`
7. **Сохраните и проверьте**

---

## 💡 Альтернатива

Если Properties действительно не могут быть добавлены, используйте `AppDataStore` (см. `ALTERNATIVE_SOLUTION.md`).

---

**Попробуйте диагностику и сообщите результат!**

