# Проверка Properties в .mlapp файле

**Date:** 2026-01-08  
**Проблема:** Свойство `isUpdating` не может быть установлено  
**Причина:** Свойство не объявлено в секции `properties` класса

---

## ✅ Решение

### Вариант 1: Убедитесь, что Properties добавлены (рекомендуется)

Откройте `.mlapp` файл в App Designer, перейдите в **Code View** и проверьте, что в секции `properties (Access = private)` есть все свойства:

```matlab
properties (Access = private)
    % === Данные ===
    originalData        % Исходные данные (полная матрица)
    currentData         % Текущие редактируемые данные
    selectedVariable    % Выбранная переменная из workspace
    editMode = 'XY'     % Режим редактирования: 'XY'
    currentPlotType = 'columns' % 'columns' или 'rows'
    
    % === Выбор части данных ===
    selectedColumns = [] % Индексы выбранных столбцов
    selectedRows = []    % Индексы выбранных строк
    
    % === Метки ===
    rowLabels = {}      % Метки строк
    columnLabels = {}   % Метки столбцов
    
    % === Перетаскивание ===
    selectedPoint = []  % Индекс выбранной точки [curveIndex, pointIndex]
    isDragging = false  % Флаг активного перетаскивания
    dragStartPosition = [] % Начальная позиция перетаскивания [x, y]
    
    % === Состояние приложения ===
    isUpdating = false  % Флаг обновления (предотвращает циклические обновления)
end
```

**ВАЖНО:** Убедитесь, что свойство `isUpdating = false` присутствует в списке!

### Вариант 2: Использовать обновленную функцию (временное решение)

Я обновил функцию `initializeAppProperties` так, чтобы она безопасно проверяла существование свойств перед их установкой. Теперь она будет работать даже если некоторые свойства не объявлены.

---

## 🔍 Как проверить, что Properties добавлены

В Code View App Designer найдите секцию:

```matlab
properties (Access = private)
```

Убедитесь, что там есть все свойства из списка выше, особенно:
- `isUpdating = false`
- `currentData`
- `currentPlotType = 'columns'`

---

## 📝 Если Properties не добавлены

1. Скопируйте код из `PROPERTIES_TO_ADD.m`
2. Вставьте после секции `properties (Access = public)` с UI компонентами
3. Сохраните файл (Ctrl+S)
4. Перезапустите приложение

---

## ✅ Что было исправлено

1. **Обновлена `initializeAppProperties.m`** - теперь безопасно проверяет существование свойств через `isprop()`
2. **Обновлена `updateGraph.m`** - использует безопасные проверки свойств
3. **Добавлена обработка ошибок** - если свойство не существует, функция продолжит работу

---

## 🧪 Проверка

После исправления:

1. Убедитесь, что Properties добавлены в `.mlapp`
2. Сохраните файл
3. Запустите приложение:
   ```matlab
   app = TableGraphEditor;
   ```
4. Проверьте, что ошибка исчезла

---

## 💡 Рекомендация

**Лучше всего добавить все Properties в `.mlapp` файл** - это правильный способ работы с App Designer. Обновленная функция `initializeAppProperties` - это временное решение для случаев, когда свойства еще не добавлены.

---

**После добавления Properties в .mlapp файл ошибка должна исчезнуть!** ✅

