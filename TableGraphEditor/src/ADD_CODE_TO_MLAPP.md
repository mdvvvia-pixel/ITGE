# Инструкция: Добавление Properties и методов в .mlapp

**Date:** 2026-01-08  
**Status:** Готово к использованию

---

## ✅ Файл восстановлен

Файл `TableGraphEditor.mlapp` восстановлен из резервной копии.

---

## 📝 Как добавить Properties и методы

### Вариант 1: Через App Designer Code View (Рекомендуется)

1. **Откройте файл в App Designer:**
   ```matlab
   cd('TableGraphEditor/src')
   appdesigner('TableGraphEditor.mlapp')
   ```

2. **Переключитесь в Code View** (кнопка вверху)

3. **Добавьте Properties:**
   - Найдите секцию `properties (Access = public)` с UI компонентами
   - После `end` этой секции добавьте код из `PROPERTIES_TO_ADD.m`

4. **Добавьте методы:**
   - Найдите функцию `createComponents`
   - После `end` функции `createComponents` добавьте код из `METHODS_TO_ADD.m`

5. **Добавьте вызов startupFcn в конструктор:**
   - Найдите конструктор `function app = TableGraphEditor`
   - После строки `registerApp(app, app.UIFigure)` добавьте:
     ```matlab
     % Execute startup function
     runStartupFcn(app, @startupFcn)
     ```

6. **Сохраните:** Ctrl+S

---

### Вариант 2: Я могу добавить программно (экспериментально)

Если хотите, я могу попробовать добавить код программно, но это рискованно. Лучше использовать Вариант 1.

---

## 📋 Что будет добавлено

### Properties:
- `originalData` - исходные данные
- `currentData` - текущие редактируемые данные
- `selectedVariable` - выбранная переменная
- `editMode` - режим редактирования
- `currentPlotType` - тип графика
- `selectedColumns`, `selectedRows` - выделенные данные
- `rowLabels`, `columnLabels` - метки
- `selectedPoint`, `isDragging`, `dragStartPosition` - для перетаскивания
- `isUpdating` - флаг обновления

### Methods:
- `startupFcn` - инициализация при запуске
- `loadVariableFromWorkspace` - загрузка данных
- `updateVariableDropdown` - обновление dropdown
- `plotByColumns` - построение графика по столбцам
- `plotByRows` - построение графика по строкам
- `updateGraph` - обновление графика
- `validateData` - валидация данных

---

## ✅ Проверка

После добавления кода:

1. Сохраните файл (Ctrl+S)
2. Запустите приложение:
   ```matlab
   app = TableGraphEditor;
   ```
3. Проверьте, что нет ошибок компиляции

---

## 📁 Файлы для копирования

- `PROPERTIES_TO_ADD.m` - Properties для добавления
- `METHODS_TO_ADD.m` - Методы для добавления

---

## ⚠️ Важно

- Все UI компоненты уже созданы и сохранены
- Добавляйте только Properties и Methods
- Не изменяйте существующий код createComponents
- Сохраняйте резервные копии перед изменениями

---

**Готово!** После добавления кода вы сможете переходить к следующей задаче: `task_20260106_data_loading.md`

