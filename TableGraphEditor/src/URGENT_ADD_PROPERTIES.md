# ⚠️ СРОЧНО: Добавьте Properties в .mlapp файл!

**Date:** 2026-01-08  
**Проблема:** Все свойства не объявлены в классе  
**Решение:** Добавить Properties вручную в App Designer

---

## 🔴 Проблема

Все свойства (`isUpdating`, `currentData`, `originalData` и т.д.) не объявлены в секции `properties` класса `TableGraphEditor`. 

**Это критично!** Без этих свойств приложение не будет работать.

---

## ✅ Решение (5 минут)

### Шаг 1: Откройте .mlapp в App Designer

```matlab
cd('TableGraphEditor/src')
appdesigner('TableGraphEditor.mlapp')
```

### Шаг 2: Переключитесь в Code View

Нажмите кнопку **Code View** вверху окна App Designer.

### Шаг 3: Найдите секцию Properties

Прокрутите вверх и найдите секцию:

```matlab
properties (Access = public)
    UIFigure  matlab.ui.Figure
    % ... другие UI компоненты
end
```

### Шаг 4: Добавьте Properties ПОСЛЕ секции UI компонентов

**Скопируйте и вставьте этот код** сразу после `end` секции `properties (Access = public)`:

```matlab
    % Application data and state
    properties (Access = private)
        % === Данные ===
        originalData        % Исходные данные (полная матрица)
        currentData         % Текущие редактируемые данные
        selectedVariable    % Выбранная переменная из workspace
        editMode = 'XY'     % Режим редактирования: 'XY'
        currentPlotType = 'columns' % 'columns' или 'rows'
        
        % === Выбор части данных ===
        selectedColumns = [] % Индексы выбранных столбцов
        selectedRows = []    % Индексы выбранных строк
        
        % === Метки ===
        rowLabels = {}      % Метки строк
        columnLabels = {}   % Метки столбцов
        
        % === Перетаскивание ===
        selectedPoint = []  % Индекс выбранной точки [curveIndex, pointIndex]
        isDragging = false  % Флаг активного перетаскивания
        dragStartPosition = [] % Начальная позиция перетаскивания [x, y]
        
        % === Состояние приложения ===
        isUpdating = false  % Флаг обновления (предотвращает циклические обновления)
    end
```

### Шаг 5: Сохраните файл

Нажмите **Ctrl+S** (или **Cmd+S** на Mac) для сохранения.

### Шаг 6: Проверьте

Запустите приложение:

```matlab
app = TableGraphEditor;
```

**Предупреждения должны исчезнуть!**

---

## 📋 Альтернатива: Использовать готовый файл

Если удобнее, откройте файл `PROPERTIES_TO_ADD.m` и скопируйте оттуда код (без комментариев в начале).

---

## 🔍 Как проверить, что Properties добавлены

После добавления в Code View вы должны увидеть две секции:

```matlab
properties (Access = public)
    % UI компоненты
    UIFigure  matlab.ui.Figure
    tblData   matlab.ui.control.Table
    % ... и т.д.
end

properties (Access = private)
    % === Данные ===
    originalData
    currentData
    % ... и т.д.
end
```

---

## ⚠️ Важно

- **НЕ удаляйте** секцию `properties (Access = public)` с UI компонентами
- **Добавьте** новую секцию `properties (Access = private)` ПОСЛЕ неё
- **Сохраните** файл после добавления
- **Перезапустите** приложение для проверки

---

## 🐛 Если ошибки остались

1. Убедитесь, что код вставлен в правильное место (после `end` секции public properties)
2. Проверьте синтаксис (нет ли лишних запятых, скобок)
3. Сохраните файл и перезапустите MATLAB
4. Запустите проверку:
   ```matlab
   app = TableGraphEditor;
   checkAppProperties(app);  % Покажет, какие свойства отсутствуют
   ```

---

## ✅ После добавления

После добавления Properties:
- ✅ Предупреждения исчезнут
- ✅ Приложение будет работать корректно
- ✅ Все helper функции смогут использовать свойства

---

**Это обязательный шаг! Без Properties приложение не будет работать.** 🔴

