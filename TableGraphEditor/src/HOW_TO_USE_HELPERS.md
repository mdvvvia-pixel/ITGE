# Как использовать helper функции в .mlapp

**Date:** 2026-01-08  
**Status:** Готово к использованию

---

## ✅ Что создано

Все helper функции созданы в папке `helpers/`:
- `updateVariableDropdown.m`
- `loadVariableFromWorkspace.m`
- `plotByColumns.m`
- `plotByRows.m`
- `updateGraph.m`
- `validateData.m`

---

## 📝 Как использовать в .mlapp

### Вариант 1: Прямой вызов (рекомендуется)

В вашем `startupFcn` и других методах просто вызывайте функции напрямую:

```matlab
function startupFcn(app, varargin)
    % Вызов helper функции - она автоматически доступна
    updateVariableDropdown(app);
end
```

**Преимущества:**
- ✅ Просто и понятно
- ✅ Не нужно создавать обертки
- ✅ Код в .mlapp остается минимальным

### Вариант 2: Создать методы-обертки (опционально)

Если хотите иметь методы в `.mlapp` для удобства, создайте обертки:

```matlab
methods (Access = private)
    
    function loadVariableFromWorkspace(app, varName)
        % Вызов helper функции
        loadVariableFromWorkspace(app, varName);
    end
    
    function updateGraph(app)
        % Вызов helper функции
        updateGraph(app);
    end
    
    % И так далее...
end
```

**Примечание:** Это опционально, можно вызывать напрямую.

---

## 🔧 Примеры использования в callbacks

### Пример 1: Callback для dropdown переменных

```matlab
function ddVariableValueChanged(app, event)
    % Получить выбранную переменную
    varName = app.ddVariable.Value;
    
    % Пропустить, если это placeholder
    if strcmp(varName, 'Select variable...')
        return;
    end
    
    % Загрузить переменную (вызов helper функции)
    loadVariableFromWorkspace(app, varName);
    
    % Обновить график (вызов helper функции)
    updateGraph(app);
end
```

### Пример 2: Callback для типа графика

```matlab
function ddPlotTypeValueChanged(app, event)
    % Обновить тип графика
    if strcmp(app.ddPlotType.Value, 'By Columns')
        app.currentPlotType = 'columns';
    else
        app.currentPlotType = 'rows';
    end
    
    % Обновить график (вызов helper функции)
    updateGraph(app);
end
```

### Пример 3: Callback для кнопки сохранения

```matlab
function btnSaveButtonPushed(app, event)
    try
        % Валидация данных (вызов helper функции)
        if ~validateData(app, app.currentData)
            return;
        end
        
        % Сохранить в workspace
        assignin('base', app.selectedVariable, app.currentData);
        uialert(app.UIFigure, 'Данные сохранены', 'Успех');
        
    catch ME
        uialert(app.UIFigure, ME.message, 'Ошибка сохранения');
    end
end
```

---

## 📍 Пути и доступность

### Автоматический путь

App Designer автоматически добавляет папку с `.mlapp` файлом в MATLAB path. Поскольку `helpers/` находится в той же папке `src/`, функции будут доступны.

### Проверка доступности

Если функции не находятся, добавьте путь вручную в `startupFcn`:

```matlab
function startupFcn(app, varargin)
    % Добавить папку helpers в path (если нужно)
    helpersPath = fullfile(fileparts(mfilename('fullpath')), 'helpers');
    addpath(helpersPath);
    
    % Теперь можно вызывать функции
    updateVariableDropdown(app);
end
```

---

## ✅ Текущее состояние

Вы уже добавили:
- ✅ Properties из `PROPERTIES_TO_ADD.m`
- ✅ `startupFcn` с вызовом `updateVariableDropdown(app)`

Теперь можете:
- ✅ Вызывать любые helper функции напрямую из callbacks
- ✅ Не нужно добавлять методы в `.mlapp` (опционально)

---

## 📋 Следующие шаги

1. ✅ Helper функции созданы
2. ✅ Properties добавлены
3. ✅ `startupFcn` создан
4. ⏳ Создайте callbacks для UI компонентов:
   - `ddVariableValueChanged` - выбор переменной
   - `ddPlotTypeValueChanged` - выбор типа графика
   - `btnSaveButtonPushed` - сохранение данных
   - И другие по мере необходимости

---

## 💡 Рекомендации

1. **Используйте прямые вызовы** - проще и понятнее
2. **Добавляйте callbacks по мере необходимости** - не нужно все сразу
3. **Тестируйте каждую функцию отдельно** - helper функции можно тестировать независимо

---

**Готово!** Теперь у вас есть все необходимые helper функции, и вы можете использовать их в `.mlapp` файле. 🚀

