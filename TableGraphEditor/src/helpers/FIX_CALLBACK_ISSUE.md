# Исправление: callback не работает при выборе переменной

**Дата:** 2026-01-08  
**Проблема:** После выбора переменной ничего не происходит  
**Статус:** 🔧 Требуется исправление

---

## Проблема

После выбора переменной из dropdown ничего не происходит:
- Данные не загружаются
- Таблица не обновляется
- Нет видимых изменений

---

## Диагностика показала

✅ Компонент `ddVariable` найден и валиден  
✅ Список переменных заполняется правильно  
✅ `startupFcn` работает  
❌ **Метод `ddVariableValueChanged` НЕ найден**  
❌ **Callback не назначен в `ValueChangedFcn`**

---

## Причины

1. **Метод `ddVariableValueChanged` не добавлен в `.mlapp` файл**
   - Метод должен быть в секции `methods (Access = private)`

2. **Callback не назначен в Design View**
   - В Property Inspector компонента `ddVariable` должно быть установлено:
     - **ValueChangedFcn**: `@app.ddVariableValueChanged`

---

## Решение

### Шаг 1: Добавить метод в .mlapp файл

1. Откройте `TableGraphEditor.mlapp` в App Designer
2. Переключитесь в **Code View**
3. Найдите секцию `methods (Access = private)`
4. Добавьте метод `ddVariableValueChanged` из `METHODS_FOR_MLAPP.m`

**Важно:** Используйте обновленную версию с отладочным выводом!

### Шаг 2: Назначить callback в Design View

1. В App Designer переключитесь в **Design View**
2. Выберите компонент `ddVariable` (dropdown для переменных)
3. В **Property Inspector** найдите свойство **ValueChangedFcn**
4. В выпадающем списке выберите: **@app.ddVariableValueChanged**
   - Или введите вручную: `@app.ddVariableValueChanged`
5. Сохраните файл (Ctrl+S)

### Шаг 3: Проверка

После исправления:

1. Перезапустите приложение:
   ```matlab
   app = TableGraphEditor;
   ```

2. Выберите переменную из dropdown

3. В командном окне MATLAB должно появиться:
   ```
   ddVariableValueChanged вызван
   Выбранное значение: [значение]
   Обработанное значение: "[имя переменной]"
   Загрузка переменной: [имя]
   ✓ Переменная загружена успешно
   ```

4. Таблица должна обновиться данными

---

## Альтернативный способ (программный)

Если не хотите использовать Design View, можно назначить callback программно в `startupFcn`:

```matlab
function startupFcn(app, varargin)
    try
        if ~isprop(app, 'ddVariable')
            return;
        end
        
        if ~isvalid(app.ddVariable)
            return;
        end
        
        % Назначить callback программно
        app.ddVariable.ValueChangedFcn = @app.ddVariableValueChanged;
        
        % Обновить список переменных
        updateVariableDropdown(app);
        
        fprintf('✓ Инициализация завершена успешно\n');
        
    catch ME
        fprintf('Ошибка в startupFcn: %s\n', ME.message);
    end
end
```

---

## Проверка после исправления

После добавления метода и назначения callback:

1. ✅ Метод `ddVariableValueChanged` найден через `ismethod(app, 'ddVariableValueChanged')`
2. ✅ `app.ddVariable.ValueChangedFcn` не пуст
3. ✅ При выборе переменной появляется отладочный вывод
4. ✅ Данные загружаются в `app.currentData`
5. ✅ Таблица обновляется

---

## Отладочный вывод

Обновленная версия `ddVariableValueChanged` включает отладочный вывод:

```matlab
fprintf('ddVariableValueChanged вызван\n');
fprintf('Выбранное значение: %s (тип: %s)\n', ...);
fprintf('Обработанное значение: "%s"\n', varName);
fprintf('Загрузка переменной: %s\n', varName);
```

Это поможет понять, вызывается ли callback и какие значения он получает.

---

## Файлы для обновления

- ✅ `METHODS_FOR_MLAPP.m` - обновлен с отладочным выводом
- ⏳ `.mlapp` файл - нужно добавить метод и назначить callback

---

## Следующие шаги

1. Скопируйте обновленную версию `ddVariableValueChanged` в `.mlapp`
2. Назначьте callback в Design View
3. Сохраните и перезапустите приложение
4. Проверьте отладочный вывод при выборе переменной

