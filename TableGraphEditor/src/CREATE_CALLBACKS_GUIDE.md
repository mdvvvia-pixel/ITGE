# Руководство по созданию Callbacks для TableGraphEditor

**Date:** 2026-01-08  
**Purpose:** Пошаговая инструкция по созданию всех необходимых callbacks в App Designer

---

## 📋 Обзор

Это руководство описывает создание всех callbacks для UI компонентов приложения TableGraphEditor. Все callbacks используют прямые вызовы helper функций из папки `helpers/`.

---

## 🎯 Список необходимых Callbacks

### Обязательные для MVP:

1. **`startupFcn`** - инициализация при запуске ✅ (уже создан)
2. **`ddVariableValueChanged`** - выбор переменной из dropdown
3. **`ddPlotTypeValueChanged`** - выбор типа графика
4. **`btnSaveButtonPushed`** - сохранение данных в workspace
5. **`tblDataCellEdit`** - редактирование ячейки таблицы
6. **`tblDataCellSelection`** - выделение ячеек в таблице

### Опциональные (для будущих версий):

7. **`axPlotButtonDown`** - начало перетаскивания точки на графике
8. **`rbModeXYValueChanged`** - изменение режима редактирования

---

## 📝 Пошаговые инструкции

### Шаг 1: Открыть файл в App Designer

```matlab
cd('TableGraphEditor/src')
appdesigner('TableGraphEditor.mlapp')
```

### Шаг 0: ⚠️ ВАЖНО - Добавить Properties в .mlapp (ОБЯЗАТЕЛЬНО!)

**Перед созданием callbacks убедитесь, что Properties добавлены!**

1. Откройте `.mlapp` в App Designer
2. Перейдите в **Code View**
3. Найдите секцию `properties (Access = public)` с UI компонентами
4. **Скопируйте код из `PROPERTIES_TO_ADD.m`** и вставьте ПОСЛЕ секции `properties (Access = public)`
5. Сохраните файл (Ctrl+S)

**Без Properties приложение не будет работать!** См. `URGENT_ADD_PROPERTIES.md` для подробных инструкций.

---

### Шаг 1.5: Добавить путь к helpers в startupFcn

**ВАЖНО:** Перед использованием helper функций нужно добавить папку `helpers/` в MATLAB path.

В вашем `startupFcn` добавьте в начало:

```matlab
function startupFcn(app, varargin)
    % STARTUPFCN Выполняется при запуске приложения
    
    % Добавить папку helpers в path
    helpersPath = fullfile(fileparts(mfilename('fullpath')), 'helpers');
    if exist(helpersPath, 'dir')
        addpath(helpersPath);
    else
        uialert(app.UIFigure, 'Папка helpers не найдена', 'Ошибка');
        return;
    end
    
    % Инициализировать свойства приложения
    initializeAppProperties(app);
    
    % Теперь можно вызывать helper функции
    updateVariableDropdown(app);
end
```

**Альтернатива:** Используйте функцию `addHelpersToPath` из папки helpers:

```matlab
function startupFcn(app, varargin)
    % Добавить папку helpers в path
    addHelpersToPath(app);
    
    % Теперь можно вызывать helper функции
    updateVariableDropdown(app);
end
```

---

### Шаг 2: Создание Callback для Dropdown переменных

#### 2.1 Создание callback в App Designer

1. В **Design View** выберите компонент `ddVariable` (dropdown для переменных)
2. В **Property Inspector** найдите свойство **ValueChangedFcn**
3. Нажмите на кнопку **⚡** рядом с полем
4. App Designer автоматически создаст функцию `ddVariableValueChanged`

#### 2.2 Код для callback

Переключитесь в **Code View** и найдите созданную функцию. Замените её содержимое на:

```matlab
function ddVariableValueChanged(app, event)
    % DDVARIABLEVALUECHANGED Callback выбора переменной
    %   Загружает выбранную переменную из workspace
    
    % Получить выбранную переменную
    varName = app.ddVariable.Value;
    
    % Пропустить, если это placeholder
    if strcmp(varName, 'Select variable...')
        return;
    end
    
    % Загрузить переменную (вызов helper функции)
    loadVariableFromWorkspace(app, varName);
    
    % Обновить таблицу
    app.tblData.Data = app.currentData;
    
    % Обновить график (вызов helper функции)
    updateGraph(app);
end
```

#### 2.3 Проверка

- Сохраните файл (Ctrl+S)
- Запустите приложение: `app = TableGraphEditor;`
- Выберите переменную из dropdown
- Проверьте, что данные загрузились в таблицу и график

---

### Шаг 3: Создание Callback для типа графика

#### 3.1 Создание callback

1. В **Design View** выберите `ddPlotType` (dropdown для типа графика)
2. В **Property Inspector** найдите **ValueChangedFcn**
3. Нажмите **⚡** для создания callback

#### 3.2 Код для callback

```matlab
function ddPlotTypeValueChanged(app, event)
    % DDPLOTTYPEVALUECHANGED Callback выбора типа графика
    %   Перестраивает график в выбранном режиме
    
    % Обновить тип графика
    if strcmp(app.ddPlotType.Value, 'By Columns')
        app.currentPlotType = 'columns';
    else
        app.currentPlotType = 'rows';
    end
    
    % Обновить график (вызов helper функции)
    if ~isempty(app.currentData)
        updateGraph(app);
    end
end
```

#### 3.3 Проверка

- Сохраните и запустите приложение
- Загрузите данные через dropdown переменных
- Переключите тип графика
- Проверьте, что график перестроился

---

### Шаг 4: Создание Callback для кнопки сохранения

#### 4.1 Создание callback

1. В **Design View** выберите `btnSave` (кнопка сохранения)
2. В **Property Inspector** найдите **ButtonPushedFcn**
3. Нажмите **⚡** для создания callback

#### 4.2 Код для callback

```matlab
function btnSaveButtonPushed(app, event)
    % BTNSAVEBUTTONPUSHED Callback кнопки сохранения
    %   Сохраняет данные в workspace
    
    try
        % Проверить, что данные загружены
        if isempty(app.selectedVariable) || isempty(app.currentData)
            uialert(app.UIFigure, 'Нет данных для сохранения', 'Ошибка');
            return;
        end
        
        % Валидация данных (вызов helper функции)
        if ~validateData(app, app.currentData)
            return;
        end
        
        % Подтверждение сохранения
            response = uiconfirm(app.UIFigure, ...
                sprintf('Сохранить изменения в переменную ''%s''?', app.selectedVariable), ...
                'Подтверждение сохранения', ...
                'Options', {'Да', 'Нет'}, ...
                'DefaultOption', 1);
        
        if strcmp(response, 'Да')
            % Сохранить в workspace
            assignin('base', app.selectedVariable, app.currentData);
            uialert(app.UIFigure, 'Данные успешно сохранены', 'Успех', 'Icon', 'success');
        end
        
    catch ME
        uialert(app.UIFigure, sprintf('Ошибка сохранения: %s', ME.message), 'Ошибка сохранения');
    end
end
```

#### 4.3 Проверка

- Сохраните и запустите приложение
- Загрузите данные
- Внесите изменения в таблицу
- Нажмите кнопку "Save to Workspace"
- Проверьте, что данные сохранились в workspace

---

### Шаг 5: Создание Callback для редактирования таблицы

#### 5.1 Создание callback

1. В **Design View** выберите `tblData` (таблица данных)
2. В **Property Inspector** найдите **CellEditCallback**
3. Нажмите **⚡** для создания callback

#### 5.2 Код для callback

```matlab
function tblDataCellEdit(app, event)
    % TBLDATACELLEDIT Callback редактирования ячейки таблицы
    %   Обновляет данные и график при изменении таблицы
    
    try
        % Предотвратить циклические обновления
        if app.isUpdating
            return;
        end
        
        app.isUpdating = true;
        
        % Получить новые данные из таблицы
        newData = app.tblData.Data;
        
        % Проверить, что данные числовые
        if ~isnumeric(newData)
            uialert(app.UIFigure, 'Данные должны быть числовыми', 'Ошибка');
            app.tblData.Data = app.currentData; % Восстановить старые данные
            app.isUpdating = false;
            return;
        end
        
        % Обновить текущие данные
        app.currentData = newData;
        
        % Обновить график (вызов helper функции)
        updateGraph(app);
        
        app.isUpdating = false;
        
    catch ME
        app.isUpdating = false;
        uialert(app.UIFigure, sprintf('Ошибка редактирования: %s', ME.message), 'Ошибка');
        % Восстановить старые данные
        app.tblData.Data = app.currentData;
    end
end
```

#### 5.3 Проверка

- Сохраните и запустите приложение
- Загрузите данные
- Измените значение в ячейке таблицы
- Проверьте, что график обновился

---

### Шаг 6: Создание Callback для выделения в таблице

#### 6.1 Создание callback

1. В **Design View** выберите `tblData`
2. В **Property Inspector** найдите **CellSelectionCallback**
3. Нажмите **⚡** для создания callback

#### 6.2 Код для callback

```matlab
function tblDataCellSelection(app, event)
    % TBLDATACELLSELECTION Callback выделения ячеек
    %   Определяет выделенные столбцы/строки и обновляет график
    
    try
        % Получить индексы выделенных ячеек
        indices = event.Indices;
        
        if isempty(indices)
            app.selectedColumns = [];
            app.selectedRows = [];
            return;
        end
        
        % Определить выделенные столбцы и строки
        selectedRows = unique(indices(:, 1));
        selectedColumns = unique(indices(:, 2));
        
        app.selectedRows = selectedRows;
        app.selectedColumns = selectedColumns;
        
        % Если выделены только столбцы или только строки, обновить график
        % (Это будет использоваться для фильтрации данных на графике)
        % Пока просто сохраняем выделение
        
    catch ME
        fprintf('Ошибка обработки выделения: %s\n', ME.message);
    end
end
```

#### 6.3 Проверка

- Сохраните и запустите приложение
- Загрузите данные
- Выделите ячейки в таблице
- Проверьте, что выделение обрабатывается корректно

---

## 🔧 Дополнительные настройки

### Настройка таблицы для редактирования

В **Design View** выберите `tblData` и в **Property Inspector**:

1. Убедитесь, что **ColumnEditable** установлен в `true` для всех столбцов
2. Установите **ColumnFormat** в `{'numeric'}` для всех столбцов

### Настройка dropdown переменных

В **Design View** выберите `ddVariable`:

1. Установите **Items** в `{'Select variable...'}` (начальное значение)
2. Значение будет обновляться программно через `updateVariableDropdown`

### Настройка dropdown типа графика

В **Design View** выберите `ddPlotType`:

1. Установите **Items** в `{'By Columns', 'By Rows'}`
2. Установите **Value** в `'By Columns'` (по умолчанию)

---

## ✅ Чеклист создания Callbacks

Проверьте, что все callbacks созданы:

- [ ] `startupFcn` - инициализация (уже создан)
- [ ] `ddVariableValueChanged` - выбор переменной
- [ ] `ddPlotTypeValueChanged` - выбор типа графика
- [ ] `btnSaveButtonPushed` - сохранение данных
- [ ] `tblDataCellEdit` - редактирование таблицы
- [ ] `tblDataCellSelection` - выделение в таблице

---

## 🧪 Тестирование

### Тест 1: Загрузка данных

```matlab
% В workspace создайте тестовые данные
testData = rand(10, 3);

% Запустите приложение
app = TableGraphEditor;

% Выберите testData из dropdown
% Проверьте:
% - Данные загрузились в таблицу
% - График построился
```

### Тест 2: Редактирование таблицы

```matlab
% После загрузки данных
% Измените значение в ячейке таблицы
% Проверьте:
% - График обновился
```

### Тест 3: Переключение типа графика

```matlab
% После загрузки данных
% Переключите тип графика (By Columns / By Rows)
% Проверьте:
% - График перестроился
```

### Тест 4: Сохранение данных

```matlab
% После загрузки и редактирования данных
% Нажмите кнопку "Save to Workspace"
% Проверьте:
% - Появилось подтверждение
% - Данные сохранились в workspace
% - Переменная в workspace обновилась
```

---

## 🐛 Troubleshooting

### Проблема: Callback не вызывается

**Решение:**
- Убедитесь, что callback привязан к компоненту в Property Inspector
- Проверьте имя функции (должно совпадать с именем компонента)
- Сохраните файл и перезапустите приложение

### Проблема: Helper функции не найдены

**Решение:**
1. Убедитесь, что папка `helpers/` находится в той же папке, что и `.mlapp`
2. Добавьте путь в `startupFcn` (см. Шаг 1.5 выше):
   ```matlab
   function startupFcn(app, varargin)
       % Добавить папку helpers в path
       helpersPath = fullfile(fileparts(mfilename('fullpath')), 'helpers');
       if exist(helpersPath, 'dir')
           addpath(helpersPath);
       end
       
       % Теперь можно вызывать helper функции
       updateVariableDropdown(app);
   end
   ```
3. Или используйте функцию `addHelpersToPath`:
   ```matlab
   addHelpersToPath(app);
   ```
4. Проверьте, что путь добавлен:
   ```matlab
   which updateGraph  % Должен показать путь к функции
   ```

### Проблема: Ошибка при загрузке переменной

**Решение:**
- Убедитесь, что переменная существует в workspace
- Проверьте, что переменная числовая
- Проверьте размеры переменной (не должна быть пустой)

### Проблема: График не обновляется

**Решение:**
- Проверьте, что `app.currentData` не пуст
- Убедитесь, что `app.isUpdating` сбрасывается в `false`
- Проверьте вызов `updateGraph(app)`

---

## 📚 Связанные документы

- `HOW_TO_USE_HELPERS.md` - использование helper функций
- `helpers/README.md` - описание helper функций
- `APP_STRUCTURE_TEMPLATE.md` - структура приложения
- `task_20260106_data_loading.md` - задача загрузки данных

---

## 🎯 Следующие шаги

После создания всех callbacks:

1. ✅ Протестируйте все функции
2. ✅ Исправьте найденные ошибки
3. ⏳ Переходите к следующей задаче: `task_20260106_graph_plotting.md`
4. ⏳ Реализуйте перетаскивание точек: `task_20260106_drag_and_drop.md`

---

## 💡 Советы

1. **Создавайте callbacks по одному** - тестируйте каждый перед созданием следующего
2. **Используйте try-catch** - обрабатывайте ошибки во всех callbacks
3. **Добавляйте комментарии** - документируйте каждый callback
4. **Тестируйте граничные случаи** - пустые данные, неверные значения и т.д.

---

**Готово!** После создания всех callbacks ваше приложение будет полностью функциональным для MVP. 🚀

