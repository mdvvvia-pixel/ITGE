# Решение проблемы: Helper функции не найдены

**Date:** 2026-01-08  
**Проблема:** MATLAB не находит функции из папки `helpers/`  
**Решение:** Добавить папку `helpers/` в MATLAB path

---

## 🔧 Быстрое решение

### Вариант 1: Добавить в startupFcn (рекомендуется)

Откройте файл `.mlapp` в App Designer, перейдите в Code View и найдите функцию `startupFcn`. 

**Замените её на:**

```matlab
function startupFcn(app, varargin)
    % STARTUPFCN Выполняется при запуске приложения
    
    % === ВАЖНО: Добавить папку helpers в path ===
    % Получить путь к папке с .mlapp файлом
    [mlappPath, ~, ~] = fileparts(mfilename('fullpath'));
    
    % Путь к папке helpers
    helpersPath = fullfile(mlappPath, 'helpers');
    
    % Проверить существование и добавить в path
    if exist(helpersPath, 'dir')
        addpath(helpersPath);
        fprintf('✓ Папка helpers добавлена в path: %s\n', helpersPath);
    else
        uialert(app.UIFigure, ...
            sprintf('Папка helpers не найдена: %s', helpersPath), ...
            'Ошибка инициализации');
        return;
    end
    
    % Теперь можно вызывать helper функции
    updateVariableDropdown(app);
end
```

### Вариант 2: Использовать функцию addHelpersToPath

1. **Сначала добавьте путь вручную один раз:**
   ```matlab
   cd('TableGraphEditor/src/helpers')
   addpath(pwd)
   ```

2. **Затем в startupFcn используйте:**
   ```matlab
   function startupFcn(app, varargin)
       % Добавить папку helpers в path
       addHelpersToPath(app);
       
       % Теперь можно вызывать helper функции
       updateVariableDropdown(app);
   end
   ```

---

## ✅ Проверка

После добавления пути проверьте:

```matlab
% В командной строке MATLAB
which updateGraph
% Должно показать: .../TableGraphEditor/src/helpers/updateGraph.m

which loadVariableFromWorkspace
% Должно показать: .../TableGraphEditor/src/helpers/loadVariableFromWorkspace.m
```

---

## 📝 Альтернативное решение: Постоянное добавление пути

Если хотите, чтобы путь добавлялся автоматически при каждом запуске MATLAB:

1. Создайте файл `startup.m` в папке пользователя MATLAB:
   ```matlab
   % В файле startup.m
   addpath('путь/к/TableGraphEditor/src/helpers');
   ```

2. Или добавьте в `pathdef.m` (требует прав администратора)

---

## 🐛 Если проблема сохраняется

### Проверка 1: Структура папок

Убедитесь, что структура правильная:
```
TableGraphEditor/
└── src/
    ├── TableGraphEditor.mlapp
    └── helpers/
        ├── updateGraph.m
        ├── loadVariableFromWorkspace.m
        └── ... (другие функции)
```

### Проверка 2: Путь к .mlapp

Проверьте, что `mfilename('fullpath')` возвращает правильный путь:
```matlab
% В startupFcn добавьте временно:
fprintf('Путь к .mlapp: %s\n', mfilename('fullpath'));
fprintf('Путь к helpers: %s\n', helpersPath);
fprintf('Папка существует: %d\n', exist(helpersPath, 'dir'));
```

### Проверка 3: Абсолютный путь

Если относительный путь не работает, используйте абсолютный:
```matlab
% В startupFcn
helpersPath = '/Users/dimamdv/Documents/CURSOR/ITGE/TableGraphEditor/src/helpers';
if exist(helpersPath, 'dir')
    addpath(helpersPath);
end
```

---

## 💡 Рекомендации

1. **Всегда добавляйте путь в startupFcn** - это гарантирует доступность функций
2. **Используйте относительные пути** - приложение будет работать на разных компьютерах
3. **Проверяйте существование папки** - обрабатывайте ошибки gracefully

---

## 📚 Связанные документы

- `CREATE_CALLBACKS_GUIDE.md` - руководство по созданию callbacks
- `HOW_TO_USE_HELPERS.md` - использование helper функций
- `helpers/README.md` - описание helper функций

---

**После добавления пути все helper функции будут доступны!** ✅

