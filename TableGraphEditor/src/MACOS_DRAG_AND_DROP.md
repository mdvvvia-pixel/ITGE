# Особенности перетаскивания в MacOS

**Дата:** 2026-01-09  
**Проблема:** В MacOS `WindowButtonMotionFcn` может не работать надежно при перетаскивании

---

## 🔍 Проблема

В MacOS при работе с MATLAB App Designer могут быть следующие особенности:

1. **WindowButtonMotionFcn может не срабатывать** при движении мыши с нажатой кнопкой
2. **Координаты мыши** (`get(0, 'PointerLocation')`) могут быть в другой системе координат
3. **Обработка событий мыши** может отличаться от Windows/Linux

---

## ✅ Решение

Реализован комбинированный подход:

### 1. Основной механизм: WindowButtonMotionFcn
- Устанавливается в `axPlotButtonDown`
- Вызывает `dragPoint(app, src, evt)` из `helpers/dragPoint.m`
- Может не работать в MacOS, но оставлен для совместимости

### 2. Fallback механизм: Таймер
- Запускается в `axPlotButtonDown`
- Проверяет движение мыши каждые 50 мс
- Вызывает `checkMouseMovement(app, timerObj)` из `helpers/checkMouseMovement.m`
- **Этот механизм должен работать в MacOS**

---

## 🔧 Что было изменено для MacOS

### В `checkMouseMovement.m`:

1. **Определение ОС:**
   ```matlab
   if ismac
       % Специальная обработка для MacOS
   end
   ```

2. **Получение координат:**
   - В MacOS используется `axPlot.CurrentPoint` напрямую (координаты данных)
   - Для других ОС используется преобразование через `PointerLocation`

3. **Проверка движения:**
   - В MacOS проверяется движение в координатах данных (порог 0.01)
   - Для других ОС проверяется движение в пикселях (порог 1 пиксель)

4. **Сохранение позиции:**
   - В MacOS сохраняются и пиксели, и координаты данных
   - Это позволяет корректно отслеживать движение

### В `axPlotButtonDown`:

- Добавлено сохранение начальных координат данных для MacOS
- Добавлены отладочные сообщения с указанием ОС

---

## 🧪 Диагностика в MacOS

### Отладочные сообщения:

При перетаскивании в консоли должны появляться:

1. **При начале перетаскивания:**
   ```
   axPlotButtonDown вызван
   ✓ Таймер запущен
   ✓ Начальные координаты данных для MacOS: [x, y]
   ```

2. **При движении мыши (каждую секунду):**
   ```
   checkMouseMovement вызван (MacOS: 1, вызов #20)
   checkMouseMovement вызван (MacOS: 1, вызов #40)
   ...
   ```

3. **Если WindowButtonMotionFcn работает:**
   ```
   dragPoint вызван (MacOS: 1)
   ```

### Если перетаскивание не работает:

1. **Проверьте, что таймер запускается:**
   - Должно быть сообщение: `✓ Таймер запущен`

2. **Проверьте, что таймер работает:**
   - Должны появляться сообщения: `checkMouseMovement вызван (MacOS: 1, ...)`

3. **Проверьте, что координаты получаются:**
   - В `checkMouseMovement` координаты должны быть валидными

4. **Проверьте, что данные обновляются:**
   - Должны вызываться `updateDataFromGraph` и `updateGraph`

---

## 📝 Что нужно проверить

### В .mlapp файле:

1. ✅ `axPlotButtonDown` должен быть обновлен (строки ~459-716 в METHODS_FOR_MLAPP.m)
2. ❌ `dragPoint`, `stopDrag`, `checkMouseMovement` НЕ должны быть в .mlapp (вынесены в helpers/)

### В helpers/:

1. ✅ `dragPoint.m` должен существовать
2. ✅ `stopDrag.m` должен существовать
3. ✅ `checkMouseMovement.m` должен существовать
4. ✅ Папка `helpers/` должна быть в MATLAB path

---

## 🐛 Известные проблемы в MacOS

1. **WindowButtonMotionFcn может не срабатывать**
   - **Решение:** Используется таймер как fallback механизм

2. **Координаты мыши могут быть в другой системе**
   - **Решение:** В MacOS используется `CurrentPoint` напрямую

3. **Таймер может работать медленнее**
   - **Решение:** Период таймера 50 мс обеспечивает плавное обновление

---

## 🔄 Альтернативные решения (если не работает)

Если таймер тоже не работает в MacOS, можно попробовать:

1. **Увеличить период таймера** (например, до 0.1 секунды)
2. **Использовать более частые проверки** (например, каждые 25 мс)
3. **Использовать другой механизм** (например, через `ButtonMotionFcn` на axes)

---

## 📖 Ссылки

- Код таймера: `helpers/checkMouseMovement.m`
- Код dragPoint: `helpers/dragPoint.m`
- Код stopDrag: `helpers/stopDrag.m`
- Код axPlotButtonDown: `helpers/METHODS_FOR_MLAPP.m` (строки ~459-716)

