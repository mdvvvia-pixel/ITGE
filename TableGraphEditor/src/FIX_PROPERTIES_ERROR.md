# Решение ошибки: Properties не инициализированы

**Date:** 2026-01-08  
**Ошибка:** `Error in updateGraph (line 14): if app.isUpdating || isempty(app.currentData)`  
**Причина:** Properties не инициализированы при первом вызове  
**Решение:** Добавить инициализацию свойств

---

## 🔧 Быстрое решение

### Шаг 1: Добавить инициализацию в startupFcn

В вашем `startupFcn` добавьте вызов `initializeAppProperties`:

```matlab
function startupFcn(app, varargin)
    % STARTUPFCN Выполняется при запуске приложения
    
    % Добавить папку helpers в path
    helpersPath = fullfile(fileparts(mfilename('fullpath')), 'helpers');
    if exist(helpersPath, 'dir')
        addpath(helpersPath);
    else
        uialert(app.UIFigure, 'Папка helpers не найдена', 'Ошибка');
        return;
    end
    
    % === ВАЖНО: Инициализировать свойства ===
    initializeAppProperties(app);
    
    % Теперь можно вызывать helper функции
    updateVariableDropdown(app);
end
```

### Шаг 2: Проверить, что Properties добавлены

Убедитесь, что в вашем `.mlapp` файле в секции `properties (Access = private)` есть все свойства из `PROPERTIES_TO_ADD.m`:

```matlab
properties (Access = private)
    % === Данные ===
    originalData        % Исходные данные (полная матрица)
    currentData         % Текущие редактируемые данные
    selectedVariable    % Выбранная переменная из workspace
    editMode = 'XY'     % Режим редактирования: 'XY'
    currentPlotType = 'columns' % 'columns' или 'rows'
    
    % === Выбор части данных ===
    selectedColumns = [] % Индексы выбранных столбцов
    selectedRows = []    % Индексы выбранных строк
    
    % === Метки ===
    rowLabels = {}      % Метки строк
    columnLabels = {}   % Метки столбцов
    
    % === Перетаскивание ===
    selectedPoint = []  % Индекс выбранной точки [curveIndex, pointIndex]
    isDragging = false  % Флаг активного перетаскивания
    dragStartPosition = [] % Начальная позиция перетаскивания [x, y]
    
    % === Состояние приложения ===
    isUpdating = false  % Флаг обновления (предотвращает циклические обновления)
end
```

---

## ✅ Что было исправлено

1. **Создана функция `initializeAppProperties.m`** - инициализирует все свойства
2. **Обновлена `updateGraph.m`** - теперь вызывает инициализацию перед использованием
3. **Обновлена `loadVariableFromWorkspace.m`** - инициализирует свойства при загрузке
4. **Обновлен `CREATE_CALLBACKS_GUIDE.md`** - добавлен вызов инициализации в startupFcn

---

## 🧪 Проверка

После исправления:

1. Сохраните `.mlapp` файл
2. Запустите приложение:
   ```matlab
   app = TableGraphEditor;
   ```
3. Выберите переменную из dropdown
4. Проверьте, что ошибка исчезла

---

## 💡 Почему это произошло?

В App Designer свойства могут быть не инициализированы до первого использования, особенно если они не имеют значений по умолчанию. Функция `initializeAppProperties` гарантирует, что все свойства инициализированы перед использованием.

---

## 📚 Связанные файлы

- `helpers/initializeAppProperties.m` - функция инициализации
- `helpers/updateGraph.m` - обновлена для использования инициализации
- `helpers/loadVariableFromWorkspace.m` - обновлена для инициализации
- `CREATE_CALLBACKS_GUIDE.md` - обновлено руководство

---

**После добавления `initializeAppProperties(app)` в startupFcn ошибка должна исчезнуть!** ✅

