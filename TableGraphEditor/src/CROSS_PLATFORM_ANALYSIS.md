# Анализ кроссплатформенности TableGraphEditor для Windows и macOS

**Date:** 2026-01-10  
**Status:** 🔍 Analysis  
**Related Tasks:** Windows/macOS compatibility, mouse event tracking  
**Author:** System Engineer (AI Assistant)

---

## 📋 Executive Summary

Данный документ анализирует возможность работы TableGraphEditor как в Windows, так и в macOS, с фокусом на особенности реализации отслеживания положения и событий мыши для обеих операционных систем. Определены необходимые доработки программы на основе FPF-Summary и MATLAB_STYLE_GUIDE.

**Ключевые выводы:**
- ✅ Базовый функционал приложения совместим с обеими ОС
- ⚠️ Обнаружены проблемы с `WindowButtonMotionFcn` в macOS
- ✅ Реализован fallback механизм через таймер для macOS
- 🔧 Требуются доработки для полной кроссплатформенности

---

## 🔍 1. Анализ текущей реализации

### 1.1 Механизмы отслеживания мыши

**Текущая реализация использует:**
1. **WindowButtonMotionFcn** - основной механизм отслеживания движения мыши
2. **WindowButtonUpFcn** - обработка отпускания кнопки мыши
3. **Timer (fallback)** - альтернативный механизм для macOS
4. **CurrentPoint** - получение координат мыши в системе данных
5. **PointerLocation** - получение координат мыши в пикселях экрана

### 1.2 Обнаруженные проблемы

#### Проблема 1: WindowButtonMotionFcn в macOS
- **Симптом:** `WindowButtonMotionFcn` может не срабатывать надежно в macOS при движении мыши с нажатой кнопкой
- **Причина:** Различия в обработке событий мыши между Windows и macOS на уровне MATLAB App Designer
- **Текущее решение:** Реализован fallback механизм через таймер с периодом 50 мс

#### Проблема 2: Координаты мыши
- **Windows:** `get(0, 'PointerLocation')` возвращает координаты относительно экрана
- **macOS:** Координаты могут быть в другой системе отсчета, требуют преобразования
- **Текущее решение:** Используется `CurrentPoint` из axes напрямую для macOS

#### Проблема 3: Преобразование координат
- **Windows:** Преобразование через `PointerLocation` и позицию figure работает надежно
- **macOS:** Преобразование может быть неточным, используется прямой доступ к `CurrentPoint`

---

## 🪟 2. Особенности Windows

### 2.1 Отслеживание событий мыши

**Механизмы:**
- `WindowButtonMotionFcn` работает надежно
- `get(0, 'PointerLocation')` возвращает координаты относительно левого верхнего угла экрана
- Координаты figure (`app.UIFigure.Position`) используются для преобразования в координаты окна
- `CurrentPoint` из axes работает стабильно

### 2.2 Система координат

```matlab
% Windows координаты:
% PointerLocation: [x_screen, y_screen] относительно экрана
% Figure Position: [x_screen, y_screen, width, height] относительно экрана
% Mouse in Figure: PointerLocation - [x_screen, y_screen]
% CurrentPoint: координаты в системе данных axes (уже преобразованы)
```

### 2.3 Проверенные функции
- ✅ `WindowButtonMotionFcn` - работает надежно
- ✅ `WindowButtonUpFcn` - работает надежно
- ✅ `get(0, 'PointerLocation')` - возвращает корректные координаты
- ✅ `axPlot.CurrentPoint` - возвращает точные координаты в системе данных

---

## 🍎 3. Особенности macOS

### 3.1 Отслеживание событий мыши

**Механизмы:**
- `WindowButtonMotionFcn` **может не работать** при движении мыши с нажатой кнопкой
- Таймер с периодом 50 мс используется как fallback механизм
- `CurrentPoint` используется напрямую (без преобразования через PointerLocation)
- Координаты данных сохраняются отдельно для отслеживания движения

### 3.2 Система координат

```matlab
% macOS координаты:
% PointerLocation: может быть в другой системе отсчета
% CurrentPoint: используется напрямую (надежнее)
% Проверка движения: в координатах данных (порог 0.01)
% Для Windows: в пикселях (порог 1 пиксель)
```

### 3.3 Известные проблемы
- ⚠️ `WindowButtonMotionFcn` может не срабатывать
- ⚠️ `get(0, 'PointerLocation')` может возвращать координаты в другой системе
- ✅ `CurrentPoint` работает надежно
- ✅ Таймер работает стабильно

### 3.4 Текущие решения
```matlab
% В checkMouseMovement.m:
if ismac
    % Используем CurrentPoint напрямую
    cp = app.axPlot.CurrentPoint;
    currentPos = cp(1, 1:2);
    % Проверяем движение в координатах данных
    mouseMoved = any(abs(currentPos - lastDataPos) > 0.01);
else
    % Для Windows используем PointerLocation
    currentMousePos = get(0, 'PointerLocation');
    mouseMoved = any(abs(currentMousePos - lastMousePos) > 1);
end
```

---

## 🔧 4. Необходимые доработки

### 4.1 Приоритет 1: Унификация обработки координат

**Задача:** Создать единую функцию для получения координат мыши, работающую на обеих ОС.

**Реализация:**
```matlab
function mousePos = getMousePosition(app)
    % GETMOUSEPOSITION Получить координаты мыши в системе данных
    %   Работает на Windows и macOS
    
    if ismac
        % macOS: используем CurrentPoint напрямую
        if isprop(app, 'axPlot') && isvalid(app.axPlot)
            cp = app.axPlot.CurrentPoint;
            mousePos = cp(1, 1:2);
        else
            mousePos = [];
        end
    else
        % Windows: используем CurrentPoint (надежнее, чем PointerLocation)
        if isprop(app, 'axPlot') && isvalid(app.axPlot)
            cp = app.axPlot.CurrentPoint;
            mousePos = cp(1, 1:2);
        else
            mousePos = [];
        end
    end
end
```

**Файлы для изменения:**
- `helpers/dragPoint.m` - заменить прямое использование `CurrentPoint`
- `helpers/checkMouseMovement.m` - использовать новую функцию
- `helpers/METHODS_FOR_MLAPP.m` (axPlotButtonDown) - использовать новую функцию

---

### 4.2 Приоритет 2: Улучшение определения ОС

**Задача:** Создать функцию для определения ОС и сохранения в свойстве app.

**Реализация:**
```matlab
function osType = detectOS()
    % DETECTOS Определить тип операционной системы
    %   Возвращает: 'Windows', 'macOS', или 'Linux'
    
    if ispc
        osType = 'Windows';
    elseif ismac
        osType = 'macOS';
    elseif isunix
        osType = 'Linux';
    else
        osType = 'Unknown';
    end
end
```

**Использование:**
- Добавить свойство `app.osType` в startupFcn
- Использовать `app.osType` вместо прямых вызовов `ismac`, `ispc`

---

### 4.3 Приоритет 3: Унификация механизма перетаскивания

**Задача:** Создать единую функцию для начала перетаскивания, работающую на обеих ОС.

**Текущая проблема:** В `axPlotButtonDown` есть специфичный для macOS код:
```matlab
% В MacOS также сохраняем начальные координаты данных
if ismac && isprop(app, 'axPlot') && isvalid(app.axPlot)
    % macOS-specific code
end
```

**Решение:** Вынести логику в отдельную функцию:
```matlab
function initializeDragTracking(app, clickPos)
    % INITIALIZEDRAGTRACKING Инициализировать отслеживание перетаскивания
    %   Работает на Windows и macOS
    
    % Сохранить начальные координаты данных
    if isprop(app, 'axPlot') && isvalid(app.axPlot)
        cp = app.axPlot.CurrentPoint;
        initialDataPos = cp(1, 1:2);
        
        % Сохранить в UserData (для доступа из таймера)
        if ~isfield(app.UIFigure.UserData, 'appData')
            app.UIFigure.UserData.appData = struct();
        end
        app.UIFigure.UserData.lastDataPosition = initialDataPos;
        app.UIFigure.UserData.initialDataPosition = initialDataPos;
        
        fprintf('✓ Начальные координаты данных: [%.4f, %.4f] (OS: %s)\n', ...
            initialDataPos(1), initialDataPos(2), app.osType);
    end
    
    % Сохранить позицию мыши для отслеживания движения
    if ismac
        % macOS: используем координаты данных
        % (уже сохранены выше)
    else
        % Windows: сохраняем пиксели для проверки движения
        currentMousePos = get(0, 'PointerLocation');
        app.UIFigure.UserData.lastMousePosition = currentMousePos;
    end
end
```

---

### 4.4 Приоритет 4: Улучшение проверки движения мыши

**Задача:** Унифицировать проверку движения мыши для обеих ОС.

**Текущая реализация в `checkMouseMovement.m`:**
- macOS: проверка в координатах данных (порог 0.01)
- Windows: проверка в пикселях (порог 1 пиксель)

**Улучшение:** Использовать координаты данных для обеих ОС (более надежно):
```matlab
function mouseMoved = checkMouseMovementInDataCoords(app, currentPos, lastPos)
    % CHECKMOUSEMOVEMENTINDATACOORDS Проверить движение мыши в координатах данных
    %   Работает на Windows и macOS
    
    if isempty(lastPos)
        mouseMoved = true; % Первый вызов
        return;
    end
    
    % Порог движения в координатах данных
    % Адаптивный порог в зависимости от масштаба графика
    xLim = app.axPlot.XLim;
    yLim = app.axPlot.YLim;
    xRange = xLim(2) - xLim(1);
    yRange = yLim(2) - yLim(1);
    avgRange = (xRange + yRange) / 2;
    
    % Порог: 0.1% от среднего диапазона, но не менее 0.001
    threshold = max(0.001, 0.001 * avgRange);
    
    mouseMoved = any(abs(currentPos - lastPos) > threshold);
end
```

---

### 4.5 Приоритет 5: Обработка ошибок при определении ОС

**Задача:** Добавить обработку ошибок для случаев, когда определение ОС не работает.

**Реализация:**
```matlab
function osType = safeDetectOS(app)
    % SAFEDETECTOS Безопасно определить ОС с обработкой ошибок
    
    try
        if isprop(app, 'osType') && ~isempty(app.osType)
            osType = app.osType;
            return;
        end
        
        osType = detectOS();
        
        % Сохранить в app (безопасно)
        if isprop(app, 'osType')
            try
                app.osType = osType;
            catch
                % Игнорировать ошибки записи
            end
        end
        
    catch ME
        % Fallback: попробовать определить через ismac/ispc
        fprintf('⚠ Ошибка определения ОС: %s\n', ME.message);
        if ismac
            osType = 'macOS';
        elseif ispc
            osType = 'Windows';
        else
            osType = 'Unknown';
        end
    end
end
```

---

## 📊 5. План реализации

### Фаза 1: Подготовка (1-2 дня)
- [ ] Создать функцию `getMousePosition.m` для унификации получения координат
- [ ] Создать функцию `detectOS.m` для определения ОС
- [ ] Создать функцию `safeDetectOS.m` с обработкой ошибок
- [ ] Протестировать функции на обеих ОС

### Фаза 2: Рефакторинг (2-3 дня)
- [ ] Обновить `dragPoint.m` для использования `getMousePosition`
- [ ] Обновить `checkMouseMovement.m` для унификации проверки движения
- [ ] Обновить `axPlotButtonDown` для использования новых функций
- [ ] Создать `initializeDragTracking.m` для инициализации перетаскивания

### Фаза 3: Тестирование (2-3 дня)
- [ ] Протестировать на Windows (Windows 10/11)
- [ ] Протестировать на macOS (macOS Monterey/Ventura/Sonoma)
- [ ] Проверить работу перетаскивания точек
- [ ] Проверить работу всех режимов редактирования
- [ ] Проверить производительность (таймер не должен замедлять работу)

### Фаза 4: Документация (1 день)
- [ ] Обновить `MACOS_DRAG_AND_DROP.md` с новыми изменениями
- [ ] Создать `CROSS_PLATFORM_TESTING.md` с результатами тестирования
- [ ] Обновить основной README с информацией о поддержке ОС

---

## 🧪 6. Рекомендации по тестированию

### 6.1 Тестовые сценарии

#### Сценарий 1: Базовое перетаскивание
1. Загрузить данные в приложение
2. Кликнуть на точку графика
3. Перетащить точку в другое место
4. Проверить обновление таблицы

**Ожидаемый результат:** Работает на Windows и macOS

#### Сценарий 2: Быстрое перетаскивание
1. Загрузить данные
2. Кликнуть на точку
3. Быстро переместить мышь (быстрые движения)
4. Проверить плавность перетаскивания

**Ожидаемый результат:** Плавное перетаскивание на обеих ОС

#### Сценарий 3: Перетаскивание за пределы axes
1. Загрузить данные
2. Кликнуть на точку
3. Перетащить мышь за пределы графика
4. Проверить поведение (должна ограничиваться пределами axes или продолжать следить)

**Ожидаемый результат:** Корректное поведение на обеих ОС

#### Сценарий 4: Переключение между окнами
1. Загрузить данные
2. Начать перетаскивание
3. Переключиться на другое окно (Alt+Tab / Cmd+Tab)
4. Вернуться к приложению
5. Проверить состояние перетаскивания

**Ожидаемый результат:** Перетаскивание корректно завершается

---

## 📝 7. Критерии приемки

### 7.1 Функциональные требования
- ✅ Перетаскивание точек работает на Windows
- ✅ Перетаскивание точек работает на macOS
- ✅ Нет потери функциональности при переходе между ОС
- ✅ Производительность не ухудшается на обеих ОС

### 7.2 Технические требования
- ✅ Код использует унифицированные функции для работы с координатами
- ✅ Определение ОС происходит один раз при запуске
- ✅ Обработка ошибок для случаев неопределенной ОС
- ✅ Документация обновлена с описанием особенностей ОС

### 7.3 Качественные требования
- ✅ Код соответствует MATLAB_STYLE_GUIDE
- ✅ Код соответствует FPF-Summary принципам
- ✅ Комментарии на русском языке для всех функций
- ✅ Обработка ошибок во всех критичных местах

---

## 🔗 8. Связанные документы

- `MACOS_DRAG_AND_DROP.md` - Особенности перетаскивания в macOS
- `DRAG_AND_DROP_IMPROVEMENTS.md` - Улучшения механизма перетаскивания
- `MATLAB_STYLE_GUIDE.md` - Руководство по стилю кода
- `FPF-Summary.md` - First Principles Framework
- `task_20260106_drag_and_drop.md` - Исходная задача по перетаскиванию

---

## 📅 9. История изменений

| Date | Author | Changes |
|------|--------|---------|
| 2026-01-10 | System Engineer | Создан документ с анализом кроссплатформенности |

---

**Status:** 🔍 Analysis Complete  
**Next Steps:** Реализация доработок согласно плану (Фаза 1)

