# Исправления для TableGraphEditor_exported.m

**Дата:** 2026-01-08

---

## 🔴 Критические исправления

### Исправление 1: Строка 142 - isfield → isprop

**Было:**
```matlab
if isfield(app, 'UIFigure') && isvalid(app.UIFigure)
```

**Должно быть:**
```matlab
if isprop(app, 'UIFigure') && isvalid(app.UIFigure)
```

**Причина:** В App Designer компоненты - это свойства, `isfield` всегда вернет `false`.

---

### Исправление 2: Строка 82 - Добавить проверку helper функций

**Было:**
```matlab
% Обновить список переменных
updateVariableDropdown(app);
```

**Должно быть:**
```matlab
% Проверить доступность helper функции и добавить путь, если нужно
scriptPath = fileparts(mfilename('fullpath'));
helpersPath = fullfile(scriptPath, 'helpers');

if exist(helpersPath, 'dir') && ~contains(path, helpersPath)
    addpath(helpersPath);
    fprintf('✓ Путь helpers/ добавлен\n');
end

if exist('updateVariableDropdown', 'file') == 2
    % Обновить список переменных
    updateVariableDropdown(app);
else
    fprintf('⚠ Функция updateVariableDropdown не найдена в path\n');
end
```

---

### Исправление 3: Строка 51-82 - Добавить проверку и назначение callback

**Добавить после строки 79 (после проверки валидности):**
```matlab
% Назначить callback программно (если не назначен)
if ismethod(app, 'ddVariableValueChanged')
    if isempty(app.ddVariable.ValueChangedFcn)
        fprintf('Назначение callback ddVariableValueChanged программно...\n');
        app.ddVariable.ValueChangedFcn = createCallbackFcn(app, @ddVariableValueChanged, true);
        fprintf('✓ Callback назначен\n');
    else
        fprintf('✓ Callback уже назначен\n');
    end
else
    fprintf('⚠ Метод ddVariableValueChanged не найден!\n');
end
```

---

## 🟡 Важные исправления

### Исправление 4: Строка 104-149 - Добавить отладочный вывод

**Добавить в начало функции `ddVariableValueChanged` (после строки 107):**
```matlab
fprintf('ddVariableValueChanged вызван\n');  % Отладочный вывод
```

**Добавить после строки 112:**
```matlab
fprintf('Выбранное значение: %s (тип: %s)\n', ...
    mat2str(varName), class(varName));
```

**Добавить после строки 123:**
```matlab
fprintf('Обработанное значение: "%s"\n', varName);
```

**Добавить после строки 129:**
```matlab
fprintf('Пропуск: значение не валидно\n');
```

**Добавить перед строкой 133:**
```matlab
fprintf('Загрузка переменной: %s\n', varName);
```

**Добавить после проверки существования функции:**
```matlab
fprintf('✓ Переменная загружена успешно\n');
```

---

### Исправление 5: Строка 133 - Добавить проверку helper функции

**Было:**
```matlab
% Загрузить переменную (helper функция сделает валидацию)
% Helper функция находится в helpers/ и автоматически в path
loadVariableFromWorkspace(app, varName);
```

**Должно быть:**
```matlab
% Проверить доступность helper функции
if exist('loadVariableFromWorkspace', 'file') == 2
    % Загрузить переменную (helper функция сделает валидацию)
    loadVariableFromWorkspace(app, varName);
    fprintf('✓ Переменная загружена успешно\n');
else
    fprintf('⚠ Функция loadVariableFromWorkspace не найдена в path\n');
    % Попытаться добавить путь
    scriptPath = fileparts(mfilename('fullpath'));
    helpersPath = fullfile(scriptPath, 'helpers');
    if exist(helpersPath, 'dir')
        addpath(helpersPath);
        fprintf('  ✓ Путь helpers/ добавлен, повторная попытка...\n');
        loadVariableFromWorkspace(app, varName);
    else
        if isprop(app, 'UIFigure') && isvalid(app.UIFigure)
            uialert(app.UIFigure, ...
                'Helper функции не найдены. Добавьте папку helpers/ в path.', ...
                'Ошибка загрузки', ...
                'Icon', 'error');
        end
    end
end
```

---

## 📋 Быстрый чеклист исправлений

- [ ] **Строка 142:** Заменить `isfield` на `isprop`
- [ ] **Строка 79-82:** Добавить проверку и назначение callback
- [ ] **Строка 82:** Добавить проверку существования `updateVariableDropdown` и автоматическое добавление пути
- [ ] **Строка 104-149:** Добавить отладочный вывод в `ddVariableValueChanged`
- [ ] **Строка 133:** Добавить проверку существования `loadVariableFromWorkspace` и автоматическое добавление пути

---

## ✅ После исправления

После внесения всех исправлений:

1. ✅ Callback будет работать корректно
2. ✅ Будет отладочный вывод для диагностики проблем
3. ✅ Приложение автоматически добавит путь к helpers, если он не найден
4. ✅ Приложение не упадет, если helper функции не в path
5. ✅ Будет понятно, что происходит при выборе переменной

---

## 🚀 Следующие шаги

1. Скопируйте исправления в файл `TableGraphEditor_exported.m`
2. Сохраните файл
3. Протестируйте приложение
4. Проверьте отладочный вывод в командном окне MATLAB

