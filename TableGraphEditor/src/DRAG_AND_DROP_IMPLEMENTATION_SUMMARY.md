# Итоги реализации перетаскивания точек на графике

**Дата:** 2026-01-09  
**Статус:** ✅ Реализовано и протестировано  
**Задача:** `task_20260106_drag_and_drop.md`

---

## 🎯 Результат

**Перетаскивание точек на графике полностью реализовано и работает!**

Пользователь может:
- Кликнуть на точку на графике
- Перетащить точку мышью
- Видеть обновление данных в реальном времени
- Автоматически обновлять таблицу после перетаскивания

---

## 📁 Структура файлов

### Helper функции (в `helpers/`):

1. **`findClosestPoint.m`**
   - Находит ближайшую точку на графике к позиции клика
   - Использует евклидово расстояние в координатах данных
   - Адаптивный порог расстояния (5% от диагонали диапазона данных)

2. **`updateDataFromGraph.m`**
   - Обновляет данные в `app.currentData` на основе новой позиции точки
   - Поддерживает режимы "по столбцам" и "по строкам"
   - Учитывает выделенные столбцы/строки

3. **`dragPoint.m`**
   - Обновляет позицию точки при движении мыши
   - Вызывается через `WindowButtonMotionFcn` или таймер
   - Использует `drawnow` для обновления графика

4. **`stopDrag.m`**
   - Завершает перетаскивание
   - Обновляет таблицу
   - Очищает callbacks и таймер
   - Различает клик и перетаскивание через флаг `hasMoved`

5. **`checkMouseMovement.m`**
   - Fallback механизм для MacOS
   - Проверяет движение мыши через таймер (каждые 50 мс)
   - Использует специальную обработку координат для MacOS

### Методы для `.mlapp` файла:

1. **`axPlotButtonDown`** (в `METHODS_FOR_MLAPP.m`, строки ~496-718)
   - Обработчик клика на графике (`ButtonDownFcn`)
   - Находит ближайшую точку
   - Устанавливает callbacks и запускает таймер
   - **Должен быть добавлен в `.mlapp` файл**

### Документация:

- `DRAG_FUNCTIONS_REFACTORING.md` - описание рефакторинга функций
- `MACOS_DRAG_AND_DROP.md` - особенности работы в MacOS
- `REMOVE_DRAG_METHODS_FROM_MLAPP.md` - инструкции по удалению методов
- `MLAPP_METHODS_CHECKLIST.md` - чеклист методов для `.mlapp`

---

## 🔧 Технические решения

### 1. Комбинированный подход для отслеживания движения мыши

**Проблема:** В MacOS `WindowButtonMotionFcn` может не работать надежно.

**Решение:**
- **Основной механизм:** `WindowButtonMotionFcn` → вызывает `dragPoint`
- **Fallback механизм:** Таймер (каждые 50 мс) → вызывает `checkMouseMovement` → вызывает `dragPoint`

### 2. Различение клика и перетаскивания

**Проблема:** Нужно различать простой клик и перетаскивание.

**Решение:**
- Флаг `app.hasMoved` устанавливается в `true` при движении мыши
- В `stopDrag` проверяется флаг: если `false`, то это был просто клик

### 3. Безопасная работа со свойствами app

**Проблема:** Свойства app могут быть недоступны в некоторых случаях.

**Решение:**
- Проверка `isprop(app, 'propertyName')` перед доступом
- Fallback через `app.UIFigure.UserData.appData` для хранения данных

### 4. Поддержка MacOS

**Проблема:** Координаты мыши в MacOS могут работать по-другому.

**Решение:**
- Использование `axPlot.CurrentPoint` напрямую для получения координат данных
- Проверка движения в координатах данных (порог 0.01) вместо пикселей
- Сохранение координат данных для корректного отслеживания движения

---

## 🐛 Решенные проблемы

### Проблема 1: `dragPoint` не вызывался

**Симптомы:**
- `stopDrag` вызывался сразу после клика
- Сообщение: "⚠ stopDrag: мышь не двигалась (это был просто клик, не перетаскивание)"

**Причина:** `WindowButtonMotionFcn` не срабатывал в MacOS при движении мыши с нажатой кнопкой.

**Решение:**
- Добавлен таймер как fallback механизм
- Таймер проверяет движение мыши каждые 50 мс
- Таймер вызывает `dragPoint` при обнаружении движения

### Проблема 2: Конфликт методов в `.mlapp`

**Симптомы:**
- Функции `dragPoint`, `stopDrag` не находились или конфликтовали

**Причина:** Методы были в `.mlapp` и в `helpers/`, что вызывало конфликт.

**Решение:**
- Вынесены `dragPoint`, `stopDrag`, `checkMouseMovement` в отдельные файлы в `helpers/`
- В `.mlapp` остался только `axPlotButtonDown`
- Callbacks вызывают функции из `helpers/` напрямую

### Проблема 3: Таймер не работал

**Симптомы:**
- Таймер запускался, но `checkMouseMovement` не вызывался

**Причина:** 
- `app` не был доступен из callback таймера
- `isDragging` не был установлен правильно

**Решение:**
- Сохранение `app` в `UserData` таймера
- Улучшенная диагностика в `checkMouseMovement`
- Проверка состояния таймера в `axPlotButtonDown`

---

## 📊 Производительность

### Оптимизации:

1. **`drawnow limitrate` → `drawnow`**
   - Изменено для более надежного обновления во время перетаскивания
   - Обеспечивает плавное движение точки

2. **Таймер с периодом 50 мс**
   - Баланс между отзывчивостью и производительностью
   - `BusyMode: 'drop'` - пропускает вызовы, если предыдущий еще выполняется

3. **Проверка движения перед обновлением**
   - Обновление происходит только при реальном движении мыши
   - Экономит ресурсы при неподвижной мыши

---

## ✅ Чеклист реализации

- [x] Функция `findClosestPoint` реализована
- [x] Функция `updateDataFromGraph` реализована
- [x] Функция `dragPoint` реализована (в `helpers/`)
- [x] Функция `stopDrag` реализована (в `helpers/`)
- [x] Функция `checkMouseMovement` реализована (в `helpers/`)
- [x] Callback `axPlotButtonDown` реализован (в `METHODS_FOR_MLAPP.m`)
- [x] Поддержка режимов "по столбцам" и "по строкам"
- [x] Поддержка MacOS
- [x] Fallback механизм через таймер
- [x] Различение клика и перетаскивания
- [x] Безопасная работа со свойствами app
- [x] Обработка ошибок во всех функциях
- [x] Отладочные сообщения для диагностики
- [x] Документация создана
- [x] **Протестировано пользователем - работает!**

---

## 📝 Инструкции для пользователя

### Что нужно сделать в `.mlapp` файле:

1. **Добавить метод `axPlotButtonDown`:**
   - Откройте `TableGraphEditor.mlapp` в App Designer
   - Перейдите в Code View
   - Найдите секцию `methods (Access = private)`
   - Скопируйте функцию `axPlotButtonDown` из `METHODS_FOR_MLAPP.m` (строки ~496-718)
   - Сохраните файл

2. **Назначить ButtonDownFcn:**
   - В Design View выберите компонент `axPlot`
   - В Property Inspector найдите "ButtonDownFcn"
   - Установите: `@app.axPlotButtonDown`
   - Сохраните файл

3. **Убедиться, что helper функции доступны:**
   - Папка `helpers/` должна быть в MATLAB path
   - Или добавьте папку в path: `addpath('helpers/')`

4. **Удалить старые методы (если есть):**
   - Удалите `function dragPoint(...)` из `.mlapp`
   - Удалите `function stopDrag(...)` из `.mlapp`
   - Удалите `function checkMouseMovement(...)` из `.mlapp`
   - Эти функции теперь в `helpers/` и вызываются автоматически

### Как использовать:

1. Загрузите данные через dropdown `ddVariable`
2. Выберите столбцы/строки для отображения на графике
3. Кликните на точку на графике
4. Перетащите точку мышью
5. Отпустите кнопку мыши - данные обновятся в таблице

---

## 🔍 Диагностика

### Отладочные сообщения:

При перетаскивании в консоли должны появляться:

1. **При клике:**
   ```
   axPlotButtonDown вызван
   ✓ Найдена точка: кривая X, точка Y
   ✓ Таймер запущен успешно (Running=on)
   ✓ Callbacks установлены + таймер запущен
   ```

2. **При движении мыши:**
   ```
   ✓ checkMouseMovement вызван ПЕРВЫЙ РАЗ (MacOS: 1)
     isDraggingFlag = 1
   dragPoint вызван (MacOS: 1)
   ```

3. **При отпускании мыши:**
   ```
   stopDrag вызван
   ✓ Таблица обновлена
   ✓ Перетаскивание завершено
   ```

### Если не работает:

1. **Проверьте, что таймер запускается:**
   - Должно быть: `✓ Таймер запущен успешно (Running=on)`

2. **Проверьте, что `checkMouseMovement` вызывается:**
   - Должно быть: `✓ checkMouseMovement вызван ПЕРВЫЙ РАЗ`

3. **Проверьте, что `isDraggingFlag = 1`:**
   - Если `0`, то свойство `isDragging` не установлено правильно

4. **Проверьте, что helper функции доступны:**
   - Выполните: `which findClosestPoint`
   - Должен вернуть путь к файлу

---

## 🎓 Уроки и выводы

1. **MacOS требует специальной обработки:**
   - `WindowButtonMotionFcn` может не работать надежно
   - Таймер как fallback - необходимое решение

2. **Разделение кода на helper функции:**
   - Упрощает отладку
   - Избегает конфликтов с методами в `.mlapp`
   - Упрощает тестирование

3. **Диагностика критична:**
   - Отладочные сообщения помогли быстро найти проблему
   - Проверка состояния таймера выявила проблему с доступом к `app`

4. **Безопасная работа со свойствами:**
   - Всегда проверять `isprop` перед доступом
   - Использовать fallback через `UserData`

---

## 📚 Связанные документы

- `task_20260106_drag_and_drop.md` - исходная задача
- `DRAG_FUNCTIONS_REFACTORING.md` - описание рефакторинга
- `MACOS_DRAG_AND_DROP.md` - особенности MacOS
- `REMOVE_DRAG_METHODS_FROM_MLAPP.md` - инструкции по удалению методов
- `MLAPP_METHODS_CHECKLIST.md` - чеклист методов

---

## ✨ Заключение

Перетаскивание точек на графике полностью реализовано и протестировано. Решение включает:

- ✅ Надежную работу в MacOS через таймер
- ✅ Поддержку обоих режимов построения графика
- ✅ Безопасную работу со свойствами app
- ✅ Различение клика и перетаскивания
- ✅ Оптимизацию производительности
- ✅ Подробную диагностику

**Статус:** ✅ **ГОТОВО К ИСПОЛЬЗОВАНИЮ**

---

**Дата завершения:** 2026-01-09  
**Протестировано:** ✅ Да  
**Работает в MacOS:** ✅ Да

