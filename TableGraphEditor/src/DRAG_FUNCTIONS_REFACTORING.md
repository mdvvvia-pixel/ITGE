# Рефакторинг функций перетаскивания

**Дата:** 2026-01-09  
**Статус:** ✅ Завершено

## 📋 Что было сделано

Функции `dragPoint`, `stopDrag` и `checkMouseMovement` вынесены в отдельные файлы в папке `helpers/` для лучшей организации кода.

---

## 📁 Созданные файлы

### 1. `helpers/dragPoint.m`
- **Назначение:** Обновление позиции точки при движении мыши
- **Вызывается из:** `WindowButtonMotionFcn` (устанавливается в `axPlotButtonDown`)
- **Функционал:**
  - Проверяет, что перетаскивание активно
  - Получает координаты мыши из `axPlot.CurrentPoint`
  - Вызывает `updateDataFromGraph` для обновления данных
  - Вызывает `updateGraph` для перерисовки графика
  - Устанавливает флаг `hasMoved = true`

### 2. `helpers/stopDrag.m`
- **Назначение:** Завершение перетаскивания и обновление таблицы
- **Вызывается из:** `WindowButtonUpFcn` (устанавливается в `axPlotButtonDown`)
- **Функционал:**
  - Проверяет, что мышь двигалась (отличает клик от перетаскивания)
  - Останавливает таймер перетаскивания
  - Очищает callbacks (`WindowButtonMotionFcn`, `WindowButtonUpFcn`)
  - Обновляет таблицу с новыми данными
  - Сбрасывает флаги перетаскивания

### 3. `helpers/checkMouseMovement.m`
- **Назначение:** Проверка движения мыши через таймер (fallback механизм)
- **Вызывается из:** Таймер (запускается в `axPlotButtonDown`)
- **Функционал:**
  - Проверяет движение мыши каждые 50 мс
  - Обновляет данные, если мышь двигалась
  - Останавливается автоматически, когда перетаскивание не активно

---

## 🔄 Что нужно обновить в .mlapp файле

### ✅ Нужно обновить:

**ТОЛЬКО `axPlotButtonDown`** (строки ~459-716 в `METHODS_FOR_MLAPP.m`)

Изменения в `axPlotButtonDown`:
- Теперь вызывает `dragPoint(app, src, evt)` вместо `app.dragPoint(src, evt)`
- Теперь вызывает `stopDrag(app, src, evt)` вместо `app.stopDrag(src, evt)`
- Запускает таймер, который вызывает `checkMouseMovement(app, timerObj)`

### ❌ НЕ нужно добавлять в .mlapp:

- **dragPoint** - находится в `helpers/dragPoint.m`, вызывается как helper функция
- **stopDrag** - находится в `helpers/stopDrag.m`, вызывается как helper функция
- **checkMouseMovement** - находится в `helpers/checkMouseMovement.m`, вызывается таймером

---

## 📝 Инструкция по обновлению .mlapp

1. Откройте `TableGraphEditor.mlapp` в App Designer
2. Перейдите в **Code View**
3. Найдите метод `axPlotButtonDown` в секции `methods (Access = private)`
4. **Замените** старую версию на новую из `METHODS_FOR_MLAPP.m` (строки ~459-716)
5. **Удалите** методы `dragPoint`, `stopDrag` и `checkMouseMovement` (если они были добавлены ранее)
6. В Design View проверьте, что `axPlot.ButtonDownFcn = @app.axPlotButtonDown`
7. Сохраните файл

---

## ✅ Преимущества рефакторинга

1. **Лучшая организация кода** - функции перетаскивания в отдельных файлах
2. **Упрощение .mlapp файла** - меньше кода в основном файле приложения
3. **Легче тестировать** - можно тестировать функции отдельно
4. **Легче поддерживать** - изменения в логике перетаскивания не требуют редактирования .mlapp

---

## 🔍 Проверка работоспособности

После обновления проверьте:

1. ✅ Приложение запускается без ошибок
2. ✅ При клике на графике находится ближайшая точка
3. ✅ При перетаскивании (удерживая кнопку мыши) точка следует за курсором
4. ✅ При отпускании кнопки таблица обновляется
5. ✅ В консоли появляются отладочные сообщения

---

## 📖 Ссылки

- Код `axPlotButtonDown`: `helpers/METHODS_FOR_MLAPP.m` (строки ~459-716)
- Helper функции:
  - `helpers/dragPoint.m`
  - `helpers/stopDrag.m`
  - `helpers/checkMouseMovement.m`
- Чеклист: `MLAPP_METHODS_CHECKLIST.md`

