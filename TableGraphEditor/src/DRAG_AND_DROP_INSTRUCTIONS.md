# Инструкция по перетаскиванию точек на графике

**Дата:** 2026-01-09

## 🖱️ Правильная последовательность действий

### Перетаскивание точки:

1. **Наведите курсор мыши на точку** на графике (близко к маркеру)
2. **Нажмите и удерживайте левую кнопку мыши** на точке
3. **Не отпуская кнопку**, двигайте мышь в нужном направлении
4. **Точка должна следовать за курсором** в реальном времени
5. **Отпустите кнопку мыши**, когда точка окажется в нужном месте

### Что НЕ является перетаскиванием:

❌ **Клик → перемещение курсора → клик** - это два отдельных клика, не перетаскивание  
❌ **Быстрое нажатие и отпускание** - это просто клик, не перетаскивание

---

## 🔧 Технические детали

### Как это работает:

1. **axPlotButtonDown** срабатывает при нажатии кнопки мыши на графике
   - Находит ближайшую точку
   - Устанавливает флаг `isDragging = true`
   - Запускает таймер для отслеживания движения мыши

2. **Таймер** (каждые 50 мс) проверяет:
   - Двигается ли мышь
   - Находится ли мышь над графиком
   - Обновляет позицию точки

3. **stopDrag** срабатывает при отпускании кнопки мыши
   - Останавливает таймер
   - Обновляет таблицу
   - Очищает callbacks

### Почему используется таймер:

В MATLAB App Designer `WindowButtonMotionFcn` может не работать надежно при перетаскивании. Таймер обеспечивает альтернативный механизм отслеживания движения мыши.

---

## 🐛 Диагностика проблем

### Если перетаскивание не работает:

1. **Проверьте логи в консоли MATLAB:**
   - Должно появиться: `axPlotButtonDown вызван`
   - Должно появиться: `✓ Таймер запущен`
   - При движении мыши должны появляться сообщения об обновлении

2. **Проверьте, что методы добавлены в .mlapp:**
   - `axPlotButtonDown`
   - `dragPoint`
   - `stopDrag`
   - `checkMouseMovement` (новая функция для таймера)

3. **Проверьте назначение callback:**
   - В Design View выберите `axPlot`
   - В Property Inspector проверьте `ButtonDownFcn` → должно быть `@app.axPlotButtonDown`

### Если точка не следует за курсором:

- Убедитесь, что вы **удерживаете кнопку мыши нажатой** во время движения
- Убедитесь, что мышь находится **над графиком** (не над легендой или другими элементами)
- Проверьте, что таймер запущен (должно быть сообщение в логах)

---

## 📝 Обновления (2026-01-09)

### Добавлено:
- ✅ Таймер для отслеживания движения мыши (fallback механизм)
- ✅ Функция `checkMouseMovement` для обработки движения через таймер
- ✅ Автоматическая остановка таймера при завершении перетаскивания

### Исправлено:
- ✅ Проблема с `WindowButtonMotionFcn`, который не срабатывал в App Designer
- ✅ Улучшена обработка координат мыши

---

## 🔄 Что нужно обновить в .mlapp:

1. **axPlotButtonDown** - обновлен с поддержкой таймера
2. **stopDrag** - обновлен с остановкой таймера
3. **checkMouseMovement** - новая функция для таймера (добавить!)

Все функции находятся в `helpers/METHODS_FOR_MLAPP.m`

