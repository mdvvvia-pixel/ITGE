# Решение: Конфликт Access и SetAccess

**Date:** 2026-01-08  
**Ошибка:** "Cannot use the Access attribute when using the SetAccess or GetAccess attribute"  
**Причина:** В MATLAB нельзя использовать `Access` и `SetAccess` одновременно  
**Решение:** Использовать только `SetAccess = public`

---

## 🔴 Проблема

MATLAB не позволяет одновременно использовать:
- `Access = private` 
- `SetAccess = public`

В одном объявлении `properties`.

---

## ✅ Решение

### Используйте только SetAccess

**Неправильно:**
```matlab
properties (Access = private, SetAccess = public)  % ← ОШИБКА!
```

**Правильно:**
```matlab
properties (SetAccess = public)
```

---

## 📝 Правильный код для копирования

Скопируйте этот код в `.mlapp` файл:

```matlab
properties (SetAccess = public)
    % === Данные ===
    originalData        % Исходные данные (полная матрица)
    currentData         % Текущие редактируемые данные
    selectedVariable    % Выбранная переменная из workspace
    editMode = 'XY'     % Режим редактирования: 'XY'
    currentPlotType = 'columns' % 'columns' или 'rows'
    
    % === Выбор части данных ===
    selectedColumns = [] % Индексы выбранных столбцов
    selectedRows = []    % Индексы выбранных строк
    
    % === Метки ===
    rowLabels = {}      % Метки строк
    columnLabels = {}   % Метки столбцов
    
    % === Перетаскивание ===
    selectedPoint = []  % Индекс выбранной точки [curveIndex, pointIndex]
    isDragging = false  % Флаг активного перетаскивания
    dragStartPosition = [] % Начальная позиция перетаскивания [x, y]
    
    % === Состояние приложения ===
    isUpdating = false  % Флаг обновления (предотвращает циклические обновления)
end
```

**Или используйте файл `PROPERTIES_FINAL.m`**

---

## 📝 Пошаговая инструкция

### Шаг 1: Откройте .mlapp в App Designer

### Шаг 2: Перейдите в Code View

### Шаг 3: Найдите секцию Properties

Найдите строку с ошибкой (подчеркнута красным):

```matlab
properties (Access = private, SetAccess = public)  % ← ОШИБКА
```

### Шаг 4: Замените на

```matlab
properties (SetAccess = public)  % ← ПРАВИЛЬНО
```

**Удалите `Access = private,` полностью!**

### Шаг 5: Сохраните файл (Ctrl+S)

Ошибка должна исчезнуть.

---

## ✅ Что это означает

- `SetAccess = public` - свойства можно устанавливать извне (включая helper функции)
- Без `Access` - свойства будут иметь доступ по умолчанию (обычно public для чтения)

Это позволит helper функциям устанавливать свойства.

---

## 🧪 Проверка

После исправления:

1. Сохраните файл
2. Ошибка должна исчезнуть
3. Запустите приложение:
   ```matlab
   app = TableGraphEditor;
   ```
4. Попробуйте выбрать переменную из dropdown

---

## 💡 Альтернатива (если нужна приватность)

Если действительно нужна приватность для чтения, можно использовать методы класса:

```matlab
properties (Access = private)  % Приватные для чтения
    originalData
    % ...
end

methods (Access = public)  % Публичные методы для установки
    function setOriginalData(app, data)
        app.originalData = data;
    end
end
```

Но для простоты лучше использовать `SetAccess = public`.

---

**Удалите `Access = private,` из объявления Properties!** 🔧

