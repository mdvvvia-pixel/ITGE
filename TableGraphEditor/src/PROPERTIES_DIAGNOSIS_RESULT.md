# Результат диагностики Properties

**Date:** 2026-01-08  
**Статус:** Проблема найдена и решена

---

## 🔍 Результаты проверки

### Проверка доступа через metaclass:
- ✅ Все 13 свойств найдены
- ✅ `SetAccess: private` - свойства могут быть установлены из методов класса
- ✅ `Constant: 0` - свойства не константы
- ✅ `Dependent: 0` - свойства не вычисляемые

### Тест прямой установки:
- ❌ **Все 13 свойств НЕ могут быть установлены извне**
- ❌ Ошибка: "Setting the '...' property is not supported"

---

## 🔴 Причина проблемы

**Properties имеют `SetAccess: private`**

Это означает:
- ✅ Свойства можно устанавливать **из методов класса** (внутри `.mlapp`)
- ❌ Свойства **НЕЛЬЗЯ** устанавливать из **внешних функций** (helper функции в `helpers/`)

---

## ✅ Решение

### Изменить объявление Properties в .mlapp

**Текущее (неправильное):**
```matlab
properties (Access = private)
    originalData
    currentData
    % ...
end
```

**Правильное:**
```matlab
properties (Access = private, SetAccess = public)
    originalData
    currentData
    selectedVariable
    editMode = 'XY'
    currentPlotType = 'columns'
    selectedColumns = []
    selectedRows = []
    rowLabels = {}
    columnLabels = {}
    selectedPoint = []
    isDragging = false
    dragStartPosition = []
    isUpdating = false
end
```

**Что это означает:**
- `Access = private` - свойства приватные (не видны извне класса)
- `SetAccess = public` - свойства можно устанавливать извне (включая helper функции)

---

## 📝 Инструкция по исправлению

1. Откройте `.mlapp` в App Designer
2. Перейдите в Code View
3. Найдите `properties (Access = private)`
4. Измените на `properties (Access = private, SetAccess = public)`
5. Сохраните файл
6. Перезапустите приложение

**Или используйте код из `PROPERTIES_CORRECT.m`**

---

## ✅ После исправления

После изменения `SetAccess` все helper функции смогут устанавливать свойства, и ошибка исчезнет.

---

**Измените SetAccess в объявлении Properties!** 🔧

