# Чеклист методов для .mlapp файла

**Дата:** 2026-01-09  
**Файл:** `TableGraphEditor.mlapp`

## 📋 Инструкция по проверке

1. Откройте `TableGraphEditor.mlapp` в App Designer
2. Перейдите в **Code View**
3. Найдите секцию `methods (Access = private)`
4. Проверьте наличие каждой функции из списка ниже
5. Сравните с кодом из `METHODS_FOR_MLAPP.m`
6. Обновите или добавьте недостающие функции

---

## ✅ Список функций для проверки/обновления

### 1. Инициализация
- [ ] **startupFcn** (строки ~10-75 в METHODS_FOR_MLAPP.m)
  - Загружает список переменных из workspace
  - Заполняет dropdown
  - Назначает callback для ddVariable

### 2. Загрузка данных
- [ ] **ddVariableValueChanged** (строки ~93-156 в METHODS_FOR_MLAPP.m)
  - Обработчик выбора переменной из dropdown
  - Вызывает `loadVariableFromWorkspace`
  - Обновляет таблицу и график

### 3. Построение графиков
- [ ] **ddPlotTypeValueChanged** (строки ~159-253 в METHODS_FOR_MLAPP.m)
  - Обработчик изменения типа графика (columns/rows)
  - Обновляет `currentPlotType`
  - Перестраивает график и таблицу

### 4. Сохранение данных
- [ ] **btnSaveButtonPushed** (строки ~265-360 в METHODS_FOR_MLAPP.m)
  - Обработчик кнопки сохранения
  - Показывает диалог подтверждения
  - Вызывает `saveVariableToWorkspace`
  - Поддерживает структуры

### 5. Выделение в таблице
- [ ] **tblDataSelectionChanged** (строки ~363-425 в METHODS_FOR_MLAPP.m)
  - Обработчик изменения выделения в таблице
  - Вызывает `updateSelection`
  - Обновляет график с учетом выделения

### 6. Перетаскивание точек (НОВОЕ/ОБНОВЛЕНИЕ)
- [ ] **axPlotButtonDown** (строки ~459-716 в METHODS_FOR_MLAPP.m)
  - **ВАЖНО:** Должен быть назначен как `ButtonDownFcn` для `axPlot` в Design View
  - Начинает перетаскивание точки
  - Вызывает `findClosestPoint`
  - Устанавливает `WindowButtonMotionFcn` и `WindowButtonUpFcn`
  - Запускает таймер для отслеживания движения мыши

- [ ] **dragPoint** ❌ НЕ нужно добавлять в .mlapp!
  - **Находится в:** `helpers/dragPoint.m`
  - Вызывается автоматически через `WindowButtonMotionFcn`
  - Обновляет позицию точки при движении мыши
  - Вызывает `updateDataFromGraph` и `updateGraph`

- [ ] **stopDrag** ❌ НЕ нужно добавлять в .mlapp!
  - **Находится в:** `helpers/stopDrag.m`
  - Вызывается автоматически через `WindowButtonUpFcn`
  - Завершает перетаскивание
  - Обновляет таблицу
  - Останавливает таймер и очищает callbacks

- [ ] **checkMouseMovement** ❌ НЕ нужно добавлять в .mlapp!
  - **Находится в:** `helpers/checkMouseMovement.m`
  - Вызывается таймером каждые 50 мс
  - Fallback механизм для отслеживания движения мыши

---

## 🔧 Настройки в Design View

### Для axPlotButtonDown:
1. Выберите компонент `axPlot` в Design View
2. В Property Inspector найдите **"ButtonDownFcn"**
3. Установите: `@app.axPlotButtonDown`
4. Сохраните файл

### Для других callbacks:
- `ddVariable` → `ValueChangedFcn` → `@app.ddVariableValueChanged`
- `ddPlotType` → `ValueChangedFcn` → `@app.ddPlotTypeValueChanged`
- `btnSave` → `ButtonPushedFcn` → `@app.btnSaveButtonPushed`
- `tblData` → `SelectionChangedFcn` → `@app.tblDataSelectionChanged`

---

## 📝 Примечания

1. **dragPoint, stopDrag и checkMouseMovement** вынесены в отдельные файлы:
   - `helpers/dragPoint.m` - вызывается через `WindowButtonMotionFcn`
   - `helpers/stopDrag.m` - вызывается через `WindowButtonUpFcn`
   - `helpers/checkMouseMovement.m` - вызывается таймером
   - **НЕ нужно добавлять эти методы в .mlapp файл!**
   - Они вызываются как helper функции из callbacks в `axPlotButtonDown`

2. **Helper функции** вызываются из методов:
   - `findClosestPoint` - поиск ближайшей точки
   - `updateDataFromGraph` - обновление данных из графика
   - `updateGraph` - перерисовка графика
   - `dragPoint` - обновление позиции точки при движении
   - `stopDrag` - завершение перетаскивания
   - `checkMouseMovement` - проверка движения мыши через таймер
   - Все находятся в папке `helpers/`

---

## 🚨 Текущий статус (2026-01-09)

### Требуют обновления/добавления:
- ✅ **axPlotButtonDown** - нужно обновить (добавлена поддержка таймера)
- ❌ **dragPoint** - вынесен в `helpers/dragPoint.m`, НЕ нужно добавлять в .mlapp
- ❌ **stopDrag** - вынесен в `helpers/stopDrag.m`, НЕ нужно добавлять в .mlapp
- ❌ **checkMouseMovement** - вынесен в `helpers/checkMouseMovement.m`, НЕ нужно добавлять в .mlapp

### Последние изменения (2026-01-09):
- ✅ **dragPoint, stopDrag, checkMouseMovement** вынесены в отдельные файлы в `helpers/`
- ✅ Добавлена поддержка таймера для отслеживания движения мыши (fallback механизм)
- ✅ Добавлен флаг `hasMoved` для отличия клика от перетаскивания
- ✅ Улучшена обработка ошибок и отладочные сообщения
- ✅ Упрощена структура: в .mlapp нужно только `axPlotButtonDown`

---

## 📖 Ссылки

- Полный код методов: `helpers/METHODS_FOR_MLAPP.m`
- Helper функции для перетаскивания:
  - `helpers/dragPoint.m`
  - `helpers/stopDrag.m`
  - `helpers/checkMouseMovement.m`
- Задача: `task_20260106_drag_and_drop.md`

