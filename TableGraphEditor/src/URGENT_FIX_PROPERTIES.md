# ⚠️ СРОЧНО: Исправление объявления Properties

**Date:** 2026-01-08  
**Ошибка:** "Cannot use the Access attribute when using the SetAccess or GetAccess attribute"  
**Решение:** Удалить `Access = private,` из объявления

---

## 🔴 Проблема

В MATLAB **нельзя** использовать `Access` и `SetAccess` одновременно!

**Неправильно:**
```matlab
properties (Access = private, SetAccess = public)  % ← ОШИБКА!
```

---

## ✅ Решение

### Удалите `Access = private,`

В App Designer Code View найдите строку 20:

```matlab
properties (Access = private, SetAccess = public)  % ← УДАЛИТЬ Access = private,
```

**Измените на:**

```matlab
properties (SetAccess = public)  % ← ТОЛЬКО SetAccess
```

---

## 📋 Полный правильный код

Скопируйте и замените всю секцию Properties:

```matlab
properties (SetAccess = public)
    % === Данные ===
    originalData        % Исходные данные (полная матрица)
    currentData         % Текущие редактируемые данные
    selectedVariable    % Выбранная переменная из workspace
    editMode = 'XY'     % Режим редактирования: 'XY'
    currentPlotType = 'columns' % 'columns' или 'rows'
    
    % === Выбор части данных ===
    selectedColumns = [] % Индексы выбранных столбцов
    selectedRows = []    % Индексы выбранных строк
    
    % === Метки ===
    rowLabels = {}      % Метки строк
    columnLabels = {}   % Метки столбцов
    
    % === Перетаскивание ===
    selectedPoint = []  % Индекс выбранной точки [curveIndex, pointIndex]
    isDragging = false  % Флаг активного перетаскивания
    dragStartPosition = [] % Начальная позиция перетаскивания [x, y]
    
    % === Состояние приложения ===
    isUpdating = false  % Флаг обновления (предотвращает циклические обновления)
end
```

**Или используйте файл `PROPERTIES_FINAL.m`**

---

## 📝 Пошаговая инструкция

1. **В App Designer Code View** найдите строку 20 с ошибкой
2. **Удалите** `Access = private,` (включая запятую)
3. **Оставьте** только `properties (SetAccess = public)`
4. **Сохраните** файл (Ctrl+S)
5. **Ошибка должна исчезнуть**

---

## ✅ После исправления

После изменения:
- ✅ Ошибка исчезнет
- ✅ Helper функции смогут устанавливать свойства
- ✅ Приложение будет работать

---

**Удалите `Access = private,` из строки 20!** 🔧

