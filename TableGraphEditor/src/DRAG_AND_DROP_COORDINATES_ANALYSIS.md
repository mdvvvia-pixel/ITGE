# Анализ проблемы координат мыши при разных масштабах осей

**Date:** 2026-01-10  
**Status:** 🔍 Analysis  
**Related Task:** `task_20260106_drag_and_drop.md`  
**Author:** System Engineer (AI Assistant)

---

## 📋 Executive Summary

В процессе решения задачи перетаскивания точек на графике выявлена необходимость улучшения определения координат мыши относительно экрана, когда масштаб графика по осям X и Y сильно отличается (например, X: 0-1000, Y: 0-1).

**Ключевые проблемы:**
- `CurrentPoint` возвращает координаты в системе данных, но при разных масштабах может быть неточным
- Нужно учитывать соотношение сторон axes и размер в пикселях для точного преобразования
- Требуется более точный метод преобразования пиксельных координат в координаты данных

---

## 🔍 1. Анализ текущей реализации

### 1.1 Текущий подход

**Используемые методы:**
1. **`CurrentPoint`** - основной метод получения координат мыши в системе данных
2. **Нормализация координат** - используется в `findClosestPoint.m` для учета разных масштабов
3. **`PointerLocation`** - используется в `checkMouseMovement.m` для отслеживания движения (Windows)

**Файлы:**
- `helpers/dragPoint.m` - использует `CurrentPoint` напрямую
- `helpers/findClosestPoint.m` - использует нормализованные координаты
- `helpers/checkMouseMovement.m` - использует `PointerLocation` и `CurrentPoint`

### 1.2 Выявленные проблемы

#### Проблема 1: Неточность CurrentPoint при разных масштабах
- **Симптом:** При сильном различии масштабов осей (например, X: 0-1000, Y: 0-1) `CurrentPoint` может давать неточные координаты
- **Причина:** `CurrentPoint` возвращает координаты в системе данных, но не учитывает точное соотношение пикселей к единицам данных
- **Влияние:** Перетаскивание может быть неточным, особенно в направлении с большим масштабом

#### Проблема 2: Отсутствие точного преобразования пикселей в координаты данных
- **Симптом:** Нет прямого преобразования пиксельных координат мыши в координаты данных с учетом масштабов
- **Причина:** Используется `CurrentPoint`, который делает преобразование автоматически, но не всегда точно
- **Влияние:** При растяжении/сжатии окна или изменении размеров axes точность может снижаться

#### Проблема 3: Не учитывается соотношение сторон axes
- **Симптом:** При разных соотношениях сторон axes (например, широкий и низкий график) координаты могут искажаться
- **Причина:** Не учитывается, что одинаковое движение мыши в пикселях может соответствовать разным изменениям в координатах данных по X и Y
- **Влияние:** Перетаскивание может быть более точным в одном направлении, чем в другом

---

## 🔧 2. Предложения по улучшению

### 2.1 Приоритет 1: Создание функции точного преобразования координат

**Задача:** Создать функцию для точного преобразования пиксельных координат мыши в координаты данных с учетом масштабов осей и соотношения сторон.

**Реализация:**
```matlab
function dataCoords = pixelToDataCoordinates(app, pixelCoords)
    % PIXELTODATACOORDINATES Преобразовать пиксельные координаты в координаты данных
    %   DATACOORDS = PIXELTODATACOORDINATES(APP, PIXELCOORDS)
    %   Преобразует пиксельные координаты мыши в координаты данных с учетом
    %   масштабов осей и соотношения сторон axes
    %
    %   Параметры:
    %       app - объект приложения TableGraphEditor
    %       pixelCoords - [x_pixel, y_pixel] координаты мыши в пикселях относительно axes
    %
    %   Возвращает:
    %       dataCoords - [x_data, y_data] координаты в системе данных
    %
    %   Описание:
    %       - Учитывает размер axes в пикселях
    %       - Учитывает пределы осей (XLim, YLim)
    %       - Учитывает соотношение сторон для точного преобразования
    %       - Работает на Windows и macOS
    
    try
        % Проверить входные данные
        if isempty(pixelCoords) || length(pixelCoords) < 2
            dataCoords = [];
            return;
        end
        
        % Проверить наличие axes
        if ~isprop(app, 'axPlot') || ~isvalid(app.axPlot)
            dataCoords = [];
            return;
        end
        
        % Получить размер axes в пикселях
        axPosPx = getpixelposition(app.axPlot);
        axWidthPx = axPosPx(3);
        axHeightPx = axPosPx(4);
        
        % Проверить валидность размеров
        if axWidthPx <= 0 || axHeightPx <= 0
            dataCoords = [];
            return;
        end
        
        % Получить пределы осей
        xLim = app.axPlot.XLim;
        yLim = app.axPlot.YLim;
        xRange = xLim(2) - xLim(1);
        yRange = yLim(2) - yLim(1);
        
        % Проверить валидность диапазонов
        if xRange <= 0 || yRange <= 0
            dataCoords = [];
            return;
        end
        
        % Преобразовать пиксельные координаты в нормализованные (0-1)
        % pixelCoords[1] - координата X в пикселях относительно axes
        % pixelCoords[2] - координата Y в пикселях относительно axes
        normX = pixelCoords(1) / axWidthPx;
        normY = pixelCoords(2) / axHeightPx;
        
        % Ограничить нормализованные координаты диапазоном [0, 1]
        normX = max(0, min(1, normX));
        normY = max(0, min(1, normY));
        
        % Преобразовать нормализованные координаты в координаты данных
        dataX = xLim(1) + normX * xRange;
        dataY = yLim(1) + (1 - normY) * yRange; % Инвертируем Y (пиксели идут сверху вниз)
        
        dataCoords = [dataX, dataY];
        
    catch ME
        fprintf('Ошибка в pixelToDataCoordinates: %s\n', ME.message);
        dataCoords = [];
    end
end
```

**Преимущества:**
- ✅ Точное преобразование с учетом масштабов осей
- ✅ Учитывает соотношение сторон axes
- ✅ Работает с любыми масштабами (X и Y могут сильно различаться)
- ✅ Учитывает инверсию оси Y (пиксели идут сверху вниз, данные - снизу вверх)

**Файл:** `helpers/pixelToDataCoordinates.m`

---

### 2.2 Приоритет 2: Создание функции получения пиксельных координат мыши

**Задача:** Создать функцию для получения пиксельных координат мыши относительно axes (а не figure или экрана).

**Реализация:**
```matlab
function pixelCoords = getMousePixelPosition(app)
    % GETMOUSEPIXELPOSITION Получить пиксельные координаты мыши относительно axes
    %   PIXELCOORDS = GETMOUSEPIXELPOSITION(APP)
    %   Возвращает [x_pixel, y_pixel] координаты мыши в пикселях относительно axes
    %
    %   Параметры:
    %       app - объект приложения TableGraphEditor
    %
    %   Возвращает:
    %       pixelCoords - [x_pixel, y_pixel] или [] если не удалось получить
    %
    %   Описание:
    %       - Преобразует координаты мыши из системы экрана в координаты axes
    %       - Учитывает позицию figure и axes
    %       - Работает на Windows и macOS
    
    try
        % Проверить наличие компонентов
        if ~isprop(app, 'axPlot') || ~isvalid(app.axPlot) || ...
           ~isprop(app, 'UIFigure') || ~isvalid(app.UIFigure)
            pixelCoords = [];
            return;
        end
        
        % Получить позицию мыши на экране
        screenMousePos = get(0, 'PointerLocation');
        
        % Получить позицию figure на экране
        figPos = app.UIFigure.Position; % [x, y, width, height] относительно экрана
        
        % Вычислить позицию мыши относительно figure (в пикселях)
        mouseInFig = [screenMousePos(1) - figPos(1), ...
                      screenMousePos(2) - figPos(2)];
        
        % Получить позицию axes в figure (в нормализованных единицах [0-1])
        axPos = app.axPlot.Position; % [x, y, width, height] в нормализованных единицах
        
        % Получить размер figure в пикселях
        figSizePx = [figPos(3), figPos(4)]; % [width, height] в пикселях
        
        % Преобразовать позицию axes в пиксели
        axPosPx = [
            axPos(1) * figSizePx(1),  % x в пикселях
            axPos(2) * figSizePx(2),  % y в пикселях
            axPos(3) * figSizePx(1),  % width в пикселях
            axPos(4) * figSizePx(2)   % height в пикселях
        ];
        
        % Вычислить позицию мыши относительно axes (в пикселях)
        mouseInAx = [
            mouseInFig(1) - axPosPx(1),  % x относительно axes
            mouseInFig(2) - axPosPx(2)   % y относительно axes
        ];
        
        % Проверить, что мышь находится внутри axes
        if mouseInAx(1) < 0 || mouseInAx(1) > axPosPx(3) || ...
           mouseInAx(2) < 0 || mouseInAx(2) > axPosPx(4)
            % Мышь вне axes - вернуть координаты, но с предупреждением
            % (может быть полезно для перетаскивания за пределы axes)
            pixelCoords = mouseInAx;
            return;
        end
        
        pixelCoords = mouseInAx;
        
    catch ME
        % Fallback: попробовать использовать CurrentPoint и обратное преобразование
        fprintf('⚠ Ошибка в getMousePixelPosition, используем CurrentPoint: %s\n', ME.message);
        
        try
            % Использовать CurrentPoint как fallback
            cp = app.axPlot.CurrentPoint;
            dataCoords = cp(1, 1:2);
            
            % Обратное преобразование: координаты данных -> пиксели
            % (менее точно, но может работать)
            xLim = app.axPlot.XLim;
            yLim = app.axPlot.YLim;
            xRange = xLim(2) - xLim(1);
            yRange = yLim(2) - yLim(1);
            
            axPosPx = getpixelposition(app.axPlot);
            axWidthPx = axPosPx(3);
            axHeightPx = axPosPx(4);
            
            normX = (dataCoords(1) - xLim(1)) / xRange;
            normY = (dataCoords(2) - yLim(1)) / yRange;
            
            pixelX = normX * axWidthPx;
            pixelY = (1 - normY) * axHeightPx; % Инвертируем Y
            
            pixelCoords = [pixelX, pixelY];
        catch ME2
            fprintf('⚠ Ошибка в fallback преобразовании: %s\n', ME2.message);
            pixelCoords = [];
        end
    end
end
```

**Преимущества:**
- ✅ Точное определение позиции мыши относительно axes
- ✅ Работает на Windows и macOS
- ✅ Имеет fallback механизм через CurrentPoint

**Файл:** `helpers/getMousePixelPosition.m`

---

### 2.3 Приоритет 3: Улучшение dragPoint с использованием точного преобразования

**Задача:** Обновить `dragPoint.m` для использования точного преобразования координат.

**Текущая реализация:**
```matlab
% Текущий код в dragPoint.m (строка ~96):
cp = app.axPlot.CurrentPoint;
currentPos = cp(1, 1:2);
```

**Предлагаемая реализация:**
```matlab
% Получить пиксельные координаты мыши
pixelCoords = getMousePixelPosition(app);

if ~isempty(pixelCoords)
    % Преобразовать пиксели в координаты данных с учетом масштабов
    currentPos = pixelToDataCoordinates(app, pixelCoords);
else
    % Fallback: использовать CurrentPoint
    cp = app.axPlot.CurrentPoint;
    currentPos = cp(1, 1:2);
end
```

**Преимущества:**
- ✅ Более точное определение координат при разных масштабах
- ✅ Сохраняет совместимость с текущей реализацией (fallback)
- ✅ Улучшает точность перетаскивания

---

### 2.4 Приоритет 4: Улучшение checkMouseMovement для Windows

**Задача:** Обновить `checkMouseMovement.m` для использования точного преобразования координат на Windows (сейчас используется PointerLocation).

**Текущая реализация (Windows):**
```matlab
% В checkMouseMovement.m (строки ~100-108):
currentMousePos = get(0, 'PointerLocation');
mousePosInFig = currentMousePos - [figPos(1), figPos(2)];
axPos = app.axPlot.Position;
if mousePosInFig(1) >= axPos(1) && ...
   mousePosInFig(1) <= axPos(1) + axPos(3) && ...
   mousePosInFig(2) >= axPos(2) && ...
   mousePosInFig(2) <= axPos(2) + axPos(4)
    cp = app.axPlot.CurrentPoint;
    currentPos = cp(1, 1:2);
```

**Предлагаемая реализация:**
```matlab
% Получить пиксельные координаты мыши
pixelCoords = getMousePixelPosition(app);

if ~isempty(pixelCoords)
    % Преобразовать пиксели в координаты данных
    currentPos = pixelToDataCoordinates(app, pixelCoords);
else
    % Fallback: использовать CurrentPoint
    if isprop(app, 'axPlot') && isvalid(app.axPlot)
        cp = app.axPlot.CurrentPoint;
        currentPos = cp(1, 1:2);
    else
        return;
    end
end
```

**Преимущества:**
- ✅ Унифицированный подход для Windows и macOS
- ✅ Более точное преобразование координат
- ✅ Учет разных масштабов осей

---

### 2.5 Приоритет 5: Кэширование размеров axes для производительности

**Задача:** Кэшировать размеры axes и пределы осей для избежания повторных вычислений.

**Реализация:**
```matlab
function cache = cacheAxesInfo(app)
    % CACHEAXESINFO Кэшировать информацию об axes для оптимизации
    %   CACHE = CACHEAXESINFO(APP)
    %   Возвращает структуру с кэшированной информацией об axes
    %
    %   Поля cache:
    %       axPosPx - позиция и размер axes в пикселях [x, y, width, height]
    %       xLim - пределы оси X [xMin, xMax]
    %       yLim - пределы оси Y [yMin, yMax]
    %       xRange - диапазон оси X
    %       yRange - диапазон оси Y
    %       timestamp - время создания кэша (для проверки актуальности)
    
    try
        if ~isprop(app, 'axPlot') || ~isvalid(app.axPlot)
            cache = [];
            return;
        end
        
        cache.axPosPx = getpixelposition(app.axPlot);
        cache.xLim = app.axPlot.XLim;
        cache.yLim = app.axPlot.YLim;
        cache.xRange = cache.xLim(2) - cache.xLim(1);
        cache.yRange = cache.yLim(2) - cache.yLim(1);
        cache.timestamp = now;
        
    catch ME
        fprintf('Ошибка в cacheAxesInfo: %s\n', ME.message);
        cache = [];
    end
end
```

**Использование:**
- Кэшировать информацию при начале перетаскивания
- Использовать кэш для преобразования координат
- Обновлять кэш при изменении размеров окна или масштаба

**Преимущества:**
- ✅ Улучшение производительности (меньше обращений к свойствам axes)
- ✅ Консистентность данных в течение одного перетаскивания
- ✅ Уменьшение задержек при частых обновлениях

---

## 📊 3. План реализации

### Фаза 1: Создание вспомогательных функций (1-2 дня)
- [ ] Создать `pixelToDataCoordinates.m`
- [ ] Создать `getMousePixelPosition.m`
- [ ] Создать `cacheAxesInfo.m` (опционально)
- [ ] Протестировать функции на обеих ОС

### Фаза 2: Обновление существующих функций (1-2 дня)
- [ ] Обновить `dragPoint.m` для использования новых функций
- [ ] Обновить `checkMouseMovement.m` для Windows
- [ ] Обновить `axPlotButtonDown` для кэширования информации об axes
- [ ] Сохранить fallback механизмы для совместимости

### Фаза 3: Тестирование (2-3 дня)
- [ ] Протестировать с разными масштабами осей (X: 0-1000, Y: 0-1)
- [ ] Протестировать с большими различиями масштабов (X: 0-10000, Y: 0-0.1)
- [ ] Протестировать на Windows и macOS
- [ ] Проверить производительность (не должно быть задержек)

### Фаза 4: Оптимизация и документация (1 день)
- [ ] Оптимизировать частые вычисления
- [ ] Обновить документацию функций
- [ ] Добавить комментарии о работе с разными масштабами

---

## 🧪 4. Рекомендации по тестированию

### 4.1 Тестовые сценарии

#### Сценарий 1: Большое различие масштабов осей
1. Создать данные с X: 0-1000, Y: 0-1
2. Построить график
3. Перетащить точку по оси X (должно быть точно)
4. Перетащить точку по оси Y (должно быть точно)
5. Проверить, что координаты обновляются корректно

**Ожидаемый результат:** Точное перетаскивание в обоих направлениях

#### Сценарий 2: Очень большое различие масштабов
1. Создать данные с X: 0-10000, Y: 0-0.1
2. Построить график
3. Перетащить точку
4. Проверить точность координат

**Ожидаемый результат:** Точное перетаскивание даже при очень большом различии

#### Сценарий 3: Изменение размеров окна
1. Начать перетаскивание
2. Изменить размер окна во время перетаскивания
3. Проверить, что координаты остаются точными

**Ожидаемый результат:** Координаты остаются точными после изменения размера

#### Сценарий 4: Разные соотношения сторон axes
1. Создать график с широким и низким axes
2. Перетащить точку
3. Изменить размер axes
4. Повторно перетащить точку
5. Проверить точность в обоих случаях

**Ожидаемый результат:** Точное перетаскивание при любом соотношении сторон

---

## 📝 5. Критерии приемки

### 5.1 Функциональные требования
- ✅ Точное перетаскивание при разнице масштабов осей 1:100
- ✅ Точное перетаскивание при разнице масштабов осей 1:1000
- ✅ Точность не зависит от соотношения сторон axes
- ✅ Работает на Windows и macOS

### 5.2 Технические требования
- ✅ Используются новые функции для преобразования координат
- ✅ Сохранены fallback механизмы для совместимости
- ✅ Производительность не ухудшается
- ✅ Код соответствует MATLAB_STYLE_GUIDE

### 5.3 Качественные требования
- ✅ Код документирован с описанием работы с разными масштабами
- ✅ Обработка ошибок во всех критичных местах
- ✅ Соответствие FPF-Summary принципам

---

## 🔗 6. Связанные документы

- `task_20260106_drag_and_drop.md` - Исходная задача по перетаскиванию
- `CROSS_PLATFORM_ANALYSIS.md` - Анализ кроссплатформенности
- `DRAG_AND_DROP_IMPROVEMENTS.md` - Предыдущие улучшения механизма перетаскивания
- `findClosestPoint.m` - Функция поиска ближайшей точки (уже использует нормализацию)

---

## 📅 7. История изменений

| Date | Author | Changes |
|------|--------|---------|
| 2026-01-10 | System Engineer | Создан анализ проблемы координат при разных масштабах осей |

---

## 🎯 8. Выводы и рекомендации

### 8.1 Главная проблема
Текущая реализация использует `CurrentPoint`, который автоматически преобразует координаты мыши в систему данных. Однако при сильном различии масштабов осей это преобразование может быть неточным, что приводит к неточному перетаскиванию точек.

### 8.2 Решение
Создание функций для точного преобразования координат:
1. `getMousePixelPosition` - получение пиксельных координат мыши относительно axes
2. `pixelToDataCoordinates` - точное преобразование пикселей в координаты данных с учетом масштабов

### 8.3 Приоритет
**Высокий** - проблема влияет на точность основного функционала (перетаскивание точек), особенно заметна при больших различиях масштабов осей.

### 8.4 Следующие шаги
1. Реализовать функции преобразования координат (Фаза 1)
2. Обновить существующие функции для использования новых методов (Фаза 2)
3. Протестировать на реальных данных с разными масштабами (Фаза 3)

---

**Status:** 🔍 Analysis Complete  
**Next Steps:** Реализация функций преобразования координат (Фаза 1)

