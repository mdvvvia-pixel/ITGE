# Альтернативное решение: Использование AppDataStore

**Date:** 2026-01-08  
**Проблема:** Properties не могут быть добавлены в .mlapp  
**Решение:** Использовать отдельный класс AppDataStore для хранения данных

---

## 🔄 Альтернативный подход

Если Properties не могут быть добавлены в `.mlapp` файл, можно использовать отдельный класс `AppDataStore` для хранения всех данных приложения.

---

## 📝 Как использовать

### Шаг 1: Добавить AppDataStore в startupFcn

В вашем `startupFcn` создайте экземпляр AppDataStore:

```matlab
function startupFcn(app, varargin)
    % STARTUPFCN Выполняется при запуске приложения
    
    % Добавить папку helpers в path
    helpersPath = fullfile(fileparts(mfilename('fullpath')), 'helpers');
    if exist(helpersPath, 'dir')
        addpath(helpersPath);
    end
    
    % === СОЗДАТЬ AppDataStore ДЛЯ ХРАНЕНИЯ ДАННЫХ ===
    app.appData = AppDataStore();
    
    % Теперь можно вызывать helper функции
    updateVariableDropdown(app);
end
```

**ВАЖНО:** Добавьте в секцию `properties (Access = private)` только одно свойство:

```matlab
properties (Access = private)
    appData  AppDataStore  % Хранилище данных приложения
end
```

### Шаг 2: Использовать app.appData вместо прямых свойств

Во всех callbacks используйте `app.appData`:

```matlab
function ddVariableValueChanged(app, event)
    varName = app.ddVariable.Value;
    
    if strcmp(varName, 'Select variable...')
        return;
    end
    
    % Использовать app.appData вместо app.currentData
    loadVariableFromWorkspace(app, varName);
    
    % Обновить таблицу
    app.tblData.Data = app.appData.currentData;
    
    % Обновить график
    updateGraphWithDataStore(app, app.appData);
end
```

### Шаг 3: Обновить helper функции

Нужно обновить helper функции для работы с `app.appData`:

```matlab
function loadVariableFromWorkspace(app, varName)
    try
        data = evalin('base', varName);
        if ~isnumeric(data)
            uialert(app.UIFigure, 'Переменная должна быть числовой', 'Ошибка');
            return;
        end
        
        % Использовать app.appData
        app.appData.originalData = data;
        app.appData.currentData = data;
        app.appData.selectedVariable = varName;
        
        % Обновить таблицу
        app.tblData.Data = app.appData.currentData;
        
    catch ME
        uialert(app.UIFigure, sprintf('Ошибка: %s', ME.message), 'Ошибка');
    end
end
```

---

## ⚠️ Проблема с текущим подходом

**Но лучше исправить проблему с Properties!**

Скорее всего Properties добавлены неправильно. Давайте проверим:

---

## 🔍 Диагностика проблемы

### Проверка 1: Правильный синтаксис

Убедитесь, что Properties добавлены правильно:

```matlab
properties (Access = private)
    % === Данные ===
    originalData        % БЕЗ точки с запятой в конце!
    currentData
    selectedVariable
    editMode = 'XY'
    currentPlotType = 'columns'
    
    % ... остальные свойства
end
```

### Проверка 2: Проверка в MATLAB

Запустите приложение и проверьте:

```matlab
app = TableGraphEditor;
checkAppProperties(app);  % Покажет, какие свойства найдены
```

### Проверка 3: Проверка синтаксиса файла

В App Designer Code View проверьте:
- Нет ли синтаксических ошибок (подсвечены красным)
- Правильно ли закрыты все секции `end`
- Нет ли конфликтов имен

---

## ✅ Рекомендация

**Лучше исправить Properties в .mlapp**, чем использовать обходной путь с AppDataStore.

Попробуйте:
1. Удалить секцию Properties
2. Сохранить файл
3. Добавить Properties заново, скопировав точно из `PROPERTIES_TO_ADD.m`
4. Сохранить и перезапустить

---

## 📋 Если все равно не работает

Используйте AppDataStore как временное решение:
1. Добавьте только `appData` в Properties
2. Используйте `app.appData.*` везде
3. Обновите все helper функции для работы с `app.appData`

---

**Попробуйте сначала исправить Properties, а если не получится - используйте AppDataStore!**

