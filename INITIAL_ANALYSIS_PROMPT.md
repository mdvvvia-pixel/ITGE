**НАЧАЛО ПРОМПТА**
Проект: Interactive Table-Graph Editor for MATLAB

Цель: Создать MATLAB-приложение (App Designer) для редактирования числовых таблиц через интерактивное взаимодействие с графиками.

📋 Основные требования:

1. Данные и таблица:

Приложение работает с числовыми матрицами переменного размера из workspace MATLAB
Поддержка данных в столбцах и строках
Одна редактируемая таблица за сеанс
Поддержка зависимых (вычисляемых) столбцов
2. Графики:

Тип: точечный график с прямыми отрезками и маркерами (аналог Excel "Точечная с прямыми отрезками")
На один набор данных - один график с несколькими кривыми
Взаимосвязь: изменение в таблице ↔ изменение на графике
3. Интерфейс:

Главное окно App Designer с:

Таблицей данных (uitable)
Областью графика (uiaxes)
Панелью управления
Два режима редактирования:

Прямое редактирование в таблице
Интерактивное перетаскивание точек на графике
4. Функционал редактирования на графике:

Перетаскивание точек мышью с вариантами:

Только по X (закреплен Y)
Только по Y (закреплен X)
Свободно по X и Y
Добавление/удаление точек на графике
При перетаскивании точки соседние точки остаются неподвижными
Без ограничений перемещения, без привязки к сетке
5. Дополнительные функции:

Несколько вариантов графиков:

По столбцам (каждый столбец - отдельная кривая)
По строкам (каждая строка - отдельная кривая)
Настройки отображения (цвета, маркеры, толщина линий)
Кнопки управления:

Сохранить изменения в workspace
Отменить/повторить
Добавить/удалить точку
Выбрать режим редактирования (X, Y, XY)
6. Технические требования:

MATLAB R2021a или выше
App Designer (не GUIDE)
Процедурный подход в рамках App Designer
Сохранение проекта в .m файлах
Callback-интеграция с внешними программами
🔧 Требуемые компоненты App Designer:

UI Components:

uitable для отображения матрицы
uiaxes для графиков
DropDown для выбора переменной из workspace
DropDown для выбора типа графика (по столбцам/строкам)
ButtonGroup с RadioButton для выбора режима редактирования (X/Y/XY)
Кнопки:

btnSave - сохранить в workspace
btnAddPoint - добавить точку
btnDeletePoint - удалить точку
btnUndo / btnRedo
CheckBox для зависимых столбцов
Callback Functions:

Выбор переменной - загрузка данных в таблицу
Изменение данных в таблице - обновление графика
Взаимодействие с графиком:

ButtonDownFcn - начало перетаскивания
WindowButtonMotionFcn - движение мыши
WindowButtonUpFcn - окончание перетаскивания
Изменение режима редактирования - переключение X/Y/XY
Добавление/удаление точек - модификация данных
📝 Архитектурный план:

1. Структура данных:

matlab
properties (Access = private)
    originalData        % Исходные данные
    currentData         % Текущие редактируемые данные
    selectedVariable    % Выбранная переменная из workspace
    editMode = 'XY'     % Режим редактирования: 'X', 'Y', 'XY'
    history = {}        % История изменений для undo/redo
    currentPlotType = 'columns' % 'columns' или 'rows'
    dependentColumns = [] % Индексы зависимых столбцов
end
2. Основные этапы работы:

Инициализация: Загрузка списка переменных workspace
Выбор данных: Пользователь выбирает переменную → загрузка в таблицу
Построение графика: На основе выбранного типа (столбцы/строки)
Редактирование:

Изменение в таблице → обновление графика
Перетаскивание на графике → обновление таблицы
Сохранение: Запись изменений обратно в workspace
3. Алгоритм перетаскивания точек:

matlab
% Псевдокод:
1. Нажатие на точку: запомнить индекс точки и начальные координаты
2. Движение мыши: 
   - Вычислить новые координаты
   - Применить ограничения режима (X/Y/XY)
   - Обновить позицию точки на графике
3. Отпускание мыши:
   - Обновить данные в матрице
   - Добавить в историю изменений
   - Обновить таблицу
4. Обработка зависимых столбцов:

При изменении базового столбца автоматически пересчитывать зависимые
Формулы зависимостей хранить в отдельной структуре
Поддержка простых операций: +, -, *, /, sin, cos и т.д.
🎨 Пример интерфейса:

text
┌─────────────────────────────────────────────────────┐
│  Table-Graph Editor                    [×]          │
├─────────────────────────────────────────────────────┤
│ [Variable: ▼]  [Plot: By Columns ▼]  [Mode: XY ▼]  │
│                                                     │
│  ┌─────────────────┐  ┌─────────────────────────┐  │
│  │                 │  │                         │  │
│  │     ТАБЛИЦА     │  │        ГРАФИК           │  │
│  │                 │  │                         │  │
│  │                 │  │                         │  │
│  │                 │  │                         │  │
│  └─────────────────┘  └─────────────────────────┘  │
│                                                     │
│  [Add Point]  [Delete Point]  [Undo]  [Redo]       │
│                                                     │
│  [Save to Workspace]  [Close]                      │
└─────────────────────────────────────────────────────┘
🚀 MVP Функционал (первая версия):

Обязательно:

Загрузка числовой матрицы из workspace
Отображение в таблице
Построение графика (по столбцам)
Перетаскивание точек (режим XY)
Обновление таблицы при изменении графика
Кнопка сохранения в workspace
Вторая очередь:

Выбор режима редактирования (X/Y/XY)
Добавление/удаление точек
Зависимые столбцы
Undo/redo
Графики по строкам
Настройки отображения
🔗 Интеграция с внешней программой:

matlab
% Пример callback-интерфейса:
function launchTableEditor(variableList, callbackFcn)
    % variableList - список переменных для редактирования
    % callbackFcn - функция, вызываемая после сохранения
    %   callbackFcn(updatedData, variableName)
    
    app = tableGraphEditor('VariableList', variableList, ...
                           'Callback', callbackFcn);
end
📊 Тестовый сценарий:

В workspace есть матрица testData = rand(10, 3);
Запуск приложения: app = tableGraphEditor;
Выбор testData из dropdown
Построение графика (3 кривые по 10 точек)
Перетаскивание 5-й точки 2-й кривой
Проверка, что значение в таблице обновилось
Нажатие "Save to Workspace"
Проверка, что testData в workspace изменилась
🎯 Конкретные вопросы для Cursor:

"Создай MATLAB App Designer приложение с uitable и uiaxes"
"Реализуй callback для загрузки переменных из workspace в uitable"
"Напиши код построения точечного графика с прямыми отрезками из данных таблицы"
"Реализуй перетаскивание точек на графике с обновлением данных в таблице"
"Добавь режимы редактирования: только X, только Y, X и Y"
"Создай механизм добавления и удаления точек на графике"
"Реализуй систему зависимых столбцов с простыми формулами"
"Добавь кнопки undo/redo с хранением истории изменений"
⚙️ Технические детали реализации:

matlab
% Ключевые аспекты для реализации:
% 1. Использование WindowButtonMotionFcn для отслеживания мыши
% 2. Хранение индекса выбранной точки в UserData axes
% 3. Автоматическое обновление графика через drawnow limitrate
% 4. Валидация числовых данных при редактировании таблицы
% 5. Обработка зависимых столбцов через listener


### Создание шаблонов задач
Создай первые файлы задач для начальных этапов:
- task_YYYYMMDD_project_structure.md - организация структуры
- task_YYYYMMDD_core_functions.md - основные вычислительные функции
- task_YYYYMMDD_verification_framework.md - фреймворк для проверки
- task_YYYYMMDD_app_designer_ui.md - создание интерфейса


## Формат ответа

Предоставь результаты анализа в виде:
1. PROJECT_DOCS.md - полная документация проекта
2. Файлы задач для начальных этапов
3. Краткое резюме с ключевыми рекомендациями
4. Список вопросов, требующих уточнения

## Дополнительные требования

- Все технические термины на английском
- Комментарии к физическим формулам на русском
- Четкая документация единиц измерения
- Ссылки на литературу, если применимо

**КОНЕЦ ПРОМПТА**


## Как использовать эти правила:

1. **Создайте файл `.cursorrules`** в корневой папке проекта
2. **Добавьте файл `MATLAB_STYLE_GUIDE.md`** для детальных инструкций
3. **При разработке в Cursor** ссылайтесь на эти правила:
   - "Согласно правилам проекта, создай callback функцию для..."
   - "Используй соглашение об именовании из .cursorrules"
   - "Следуй структуре из MATLAB_STYLE_GUIDE.md"

Эти правила обеспечат:
- ✅ Единый стиль кода
- ✅ Предсказуемую структуру проекта
- ✅ Легкую поддержку и расширение
- ✅ Совместимость с требованиями заказчика