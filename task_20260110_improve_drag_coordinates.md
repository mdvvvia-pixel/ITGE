# Task: Улучшение точности координат при перетаскивании точек

**Date:** 2026-01-10  
**Status:** 📋 Planned  
**Priority:** High  
**Assignee:** [Имя]  
**Related Files:** 
- `TableGraphEditor/src/helpers/dragPoint.m`
- `TableGraphEditor/src/helpers/checkMouseMovement.m`
- `TableGraphEditor/src/helpers/METHODS_FOR_MLAPP.m`
- `TableGraphEditor/src/DRAG_AND_DROP_COORDINATES_ANALYSIS.md`

**Dependencies:**
- **Requires:** `task_20260106_drag_and_drop.md` (базовая реализация перетаскивания)

---

## Objective

Улучшить точность определения координат мыши при перетаскивании точек на графике, особенно при больших различиях масштабов осей (например, X: 0-1000, Y: 0-1).

**Success Criteria:**
- [ ] Точное перетаскивание при разнице масштабов осей 1:100
- [ ] Точное перетаскивание при разнице масштабов осей 1:1000
- [ ] Точность не зависит от соотношения сторон axes
- [ ] Работает на Windows и macOS
- [ ] Производительность не ухудшается

---

## Background

### Context
В процессе решения задачи перетаскивания точек (`task_20260106_drag_and_drop.md`) выявлена необходимость улучшения точности определения координат мыши относительно экрана, когда масштаб графика по осям X и Y сильно отличается.

### Problem Statement
- `CurrentPoint` возвращает координаты в системе данных автоматически
- При больших различиях масштабов осей преобразование может быть неточным
- Не учитывается точное соотношение пикселей к единицам данных по каждой оси

### Related Analysis
- **Детальный анализ:** `DRAG_AND_DROP_COORDINATES_ANALYSIS.md`
- **Кроссплатформенность:** `CROSS_PLATFORM_ANALYSIS.md`

---

## Approach

### Design Overview
1. Создать функцию точного преобразования пикселей в координаты данных
2. Создать функцию получения пиксельных координат мыши относительно axes
3. Обновить существующие функции для использования новых методов
4. Добавить кэширование для оптимизации производительности
5. Протестировать на реальных данных с разными масштабами

### Algorithm

**Pseudocode:**
```
1. Создать pixelToDataCoordinates(app, pixelCoords):
   - Получить размер axes в пикселях
   - Получить пределы осей (XLim, YLim)
   - Преобразовать пиксели в нормализованные координаты (0-1)
   - Преобразовать нормализованные координаты в координаты данных
   - Учесть инверсию оси Y

2. Создать getMousePixelPosition(app):
   - Получить координаты мыши на экране (PointerLocation)
   - Преобразовать в координаты figure
   - Преобразовать в координаты axes
   - Вернуть пиксельные координаты относительно axes

3. Обновить dragPoint:
   - Использовать getMousePixelPosition для получения пикселей
   - Использовать pixelToDataCoordinates для преобразования
   - Fallback на CurrentPoint при ошибках

4. Обновить checkMouseMovement:
   - Унифицировать подход для Windows и macOS
   - Использовать новые функции преобразования
```

---

## Implementation Plan

### Phase 1: Создание вспомогательных функций (1-2 дня)

**Goal:** Создать функции для точного преобразования координат

**Tasks:**
1. Создать функцию `pixelToDataCoordinates.m`
   - Преобразование пикселей → координаты данных
   - Учет масштабов осей и соотношения сторон
   - Инверсия оси Y
   - Обработка ошибок

2. Создать функцию `getMousePixelPosition.m`
   - Получение пиксельных координат мыши относительно axes
   - Преобразование экран → figure → axes
   - Fallback механизм через CurrentPoint
   - Обработка ошибок

3. Создать функцию `cacheAxesInfo.m` (опционально)
   - Кэширование размеров axes в пикселях
   - Кэширование пределов осей
   - Временная метка для проверки актуальности
   - Использование кэша для оптимизации

**Deliverables:**
- [ ] Функция `pixelToDataCoordinates.m` работает корректно
- [ ] Функция `getMousePixelPosition.m` работает корректно
- [ ] Функция `cacheAxesInfo.m` работает (опционально)
- [ ] Все функции протестированы на обеих ОС

**File Locations:**
- `TableGraphEditor/src/helpers/pixelToDataCoordinates.m` (новый)
- `TableGraphEditor/src/helpers/getMousePixelPosition.m` (новый)
- `TableGraphEditor/src/helpers/cacheAxesInfo.m` (новый, опционально)

---

### Phase 2: Обновление существующих функций (1-2 дня)

**Goal:** Интегрировать новые функции в существующий код

**Tasks:**
1. Обновить `dragPoint.m`
   - Заменить прямое использование `CurrentPoint`
   - Использовать `getMousePixelPosition` и `pixelToDataCoordinates`
   - Сохранить fallback на `CurrentPoint`
   - Обновить комментарии

2. Обновить `checkMouseMovement.m`
   - Заменить специфичный для Windows код
   - Использовать новые функции для унификации
   - Сохранить fallback механизмы
   - Обновить комментарии

3. Обновить `axPlotButtonDown` в `METHODS_FOR_MLAPP.m`
   - Добавить кэширование информации об axes (если используется)
   - Использовать новые функции при необходимости
   - Сохранить совместимость с текущим кодом

**Deliverables:**
- [ ] `dragPoint.m` использует новые функции
- [ ] `checkMouseMovement.m` унифицирован для Windows и macOS
- [ ] `axPlotButtonDown` обновлен (если требуется)
- [ ] Все fallback механизмы сохранены
- [ ] Код протестирован на обеих ОС

**File Locations:**
- `TableGraphEditor/src/helpers/dragPoint.m` (обновить)
- `TableGraphEditor/src/helpers/checkMouseMovement.m` (обновить)
- `TableGraphEditor/src/helpers/METHODS_FOR_MLAPP.m` (обновить при необходимости)

---

### Phase 3: Тестирование (2-3 дня)

**Goal:** Проверить точность и производительность улучшений

**Tasks:**
1. Тестирование с разными масштабами осей
   - X: 0-1000, Y: 0-1 (разница 1000:1)
   - X: 0-10000, Y: 0-0.1 (разница 100000:1)
   - X: 0-1, Y: 0-1000 (обратная разница)
   - Проверить точность перетаскивания в каждом направлении

2. Тестирование на разных ОС
   - Windows 10/11
   - macOS Monterey/Ventura/Sonoma
   - Проверить работу на обеих ОС

3. Тестирование производительности
   - Измерить время выполнения новых функций
   - Проверить, нет ли задержек при перетаскивании
   - Сравнить с текущей реализацией

4. Тестирование edge cases
   - Изменение размеров окна во время перетаскивания
   - Перетаскивание за пределы axes
   - Быстрое перетаскивание (частые обновления)
   - Разные соотношения сторон axes

**Deliverables:**
- [ ] Все тестовые сценарии пройдены успешно
- [ ] Документированы результаты тестирования
- [ ] Выявлены и исправлены проблемы (если есть)
- [ ] Производительность соответствует требованиям

**Test Files:**
- `test/test_drag_coordinates_accuracy.m` (создать)
- `test/test_drag_coordinates_performance.m` (создать)
- `test/test_drag_coordinates_cross_platform.m` (создать)

---

### Phase 4: Оптимизация и документация (1 день)

**Goal:** Оптимизировать код и обновить документацию

**Tasks:**
1. Оптимизация производительности
   - Проверить и оптимизировать частые вычисления
   - Использовать кэширование где возможно
   - Убедиться, что нет избыточных преобразований

2. Обновление документации
   - Обновить комментарии в коде
   - Обновить `DRAG_AND_DROP_COORDINATES_ANALYSIS.md` с результатами
   - Обновить `CROSS_PLATFORM_ANALYSIS.md` (если требуется)
   - Обновить основной README (если требуется)

3. Code review
   - Проверить соответствие MATLAB_STYLE_GUIDE
   - Проверить соответствие FPF-Summary принципам
   - Убедиться в обработке всех ошибок

**Deliverables:**
- [ ] Код оптимизирован для производительности
- [ ] Документация обновлена
- [ ] Code review пройден
- [ ] Все соответствует стандартам проекта

---

## Code Implementation

### Function 1: pixelToDataCoordinates

**File:** `src/helpers/pixelToDataCoordinates.m`

**Status:** 📋 Planned

**Implementation:**
```matlab
function dataCoords = pixelToDataCoordinates(app, pixelCoords)
    % PIXELTODATACOORDINATES Преобразовать пиксельные координаты в координаты данных
    %   DATACOORDS = PIXELTODATACOORDINATES(APP, PIXELCOORDS)
    %   Преобразует пиксельные координаты мыши в координаты данных с учетом
    %   масштабов осей и соотношения сторон axes
    %
    %   Параметры:
    %       app - объект приложения TableGraphEditor
    %       pixelCoords - [x_pixel, y_pixel] координаты мыши в пикселях относительно axes
    %
    %   Возвращает:
    %       dataCoords - [x_data, y_data] координаты в системе данных или [] при ошибке
    %
    %   Описание:
    %       - Учитывает размер axes в пикселях
    %       - Учитывает пределы осей (XLim, YLim)
    %       - Учитывает соотношение сторон для точного преобразования
    %       - Работает на Windows и macOS
    %       - Инвертирует ось Y (пиксели идут сверху вниз, данные - снизу вверх)
    
    % TODO: Реализовать функцию согласно спецификации в DRAG_AND_DROP_COORDINATES_ANALYSIS.md
end
```

**Test Cases:**
- [ ] Преобразование при одинаковых масштабах (X: 0-100, Y: 0-100)
- [ ] Преобразование при разных масштабах (X: 0-1000, Y: 0-1)
- [ ] Преобразование при очень больших различиях (X: 0-10000, Y: 0-0.1)
- [ ] Преобразование при координатах вне axes
- [ ] Обработка ошибок (невалидные входные данные)

---

### Function 2: getMousePixelPosition

**File:** `src/helpers/getMousePixelPosition.m`

**Status:** 📋 Planned

**Implementation:**
```matlab
function pixelCoords = getMousePixelPosition(app)
    % GETMOUSEPIXELPOSITION Получить пиксельные координаты мыши относительно axes
    %   PIXELCOORDS = GETMOUSEPIXELPOSITION(APP)
    %   Возвращает [x_pixel, y_pixel] координаты мыши в пикселях относительно axes
    %
    %   Параметры:
    %       app - объект приложения TableGraphEditor
    %
    %   Возвращает:
    %       pixelCoords - [x_pixel, y_pixel] или [] если не удалось получить
    %
    %   Описание:
    %       - Преобразует координаты мыши из системы экрана в координаты axes
    %       - Учитывает позицию figure и axes
    %       - Работает на Windows и macOS
    %       - Имеет fallback механизм через CurrentPoint
    
    % TODO: Реализовать функцию согласно спецификации в DRAG_AND_DROP_COORDINATES_ANALYSIS.md
end
```

**Test Cases:**
- [ ] Получение координат когда мышь внутри axes
- [ ] Получение координат когда мышь вне axes (должен возвращать координаты, но с предупреждением)
- [ ] Работа на Windows
- [ ] Работа на macOS
- [ ] Fallback через CurrentPoint при ошибках
- [ ] Обработка ошибок (невалидные компоненты)

---

### Function 3: cacheAxesInfo (опционально)

**File:** `src/helpers/cacheAxesInfo.m`

**Status:** 📋 Planned (опционально)

**Implementation:**
```matlab
function cache = cacheAxesInfo(app)
    % CACHEAXESINFO Кэшировать информацию об axes для оптимизации
    %   CACHE = CACHEAXESINFO(APP)
    %   Возвращает структуру с кэшированной информацией об axes
    %
    %   Параметры:
    %       app - объект приложения TableGraphEditor
    %
    %   Возвращает:
    %       cache - структура с полями:
    %           axPosPx - позиция и размер axes в пикселях [x, y, width, height]
    %           xLim - пределы оси X [xMin, xMax]
    %           yLim - пределы оси Y [yMin, yMax]
    %           xRange - диапазон оси X
    %           yRange - диапазон оси Y
    %           timestamp - время создания кэша (для проверки актуальности)
    %       или [] при ошибке
    
    % TODO: Реализовать функцию согласно спецификации в DRAG_AND_DROP_COORDINATES_ANALYSIS.md
end
```

**Test Cases:**
- [ ] Кэширование корректных данных
- [ ] Проверка актуальности кэша (timestamp)
- [ ] Обработка ошибок (невалидные компоненты)

---

### Update 1: dragPoint.m

**File:** `src/helpers/dragPoint.m`

**Status:** 📋 Planned

**Changes:**
- Заменить прямое использование `CurrentPoint` (строка ~96)
- Использовать `getMousePixelPosition` и `pixelToDataCoordinates`
- Сохранить fallback на `CurrentPoint`

**Before:**
```matlab
cp = app.axPlot.CurrentPoint;
currentPos = cp(1, 1:2);
```

**After:**
```matlab
% Получить пиксельные координаты мыши
pixelCoords = getMousePixelPosition(app);

if ~isempty(pixelCoords)
    % Преобразовать пиксели в координаты данных с учетом масштабов
    currentPos = pixelToDataCoordinates(app, pixelCoords);
    
    % Проверить валидность координат
    if isempty(currentPos) || ~isfinite(currentPos(1)) || ~isfinite(currentPos(2))
        % Fallback: использовать CurrentPoint
        cp = app.axPlot.CurrentPoint;
        currentPos = cp(1, 1:2);
    end
else
    % Fallback: использовать CurrentPoint
    cp = app.axPlot.CurrentPoint;
    currentPos = cp(1, 1:2);
end
```

**Test Cases:**
- [ ] Перетаскивание работает с новыми функциями
- [ ] Fallback работает при ошибках
- [ ] Точность улучшена при разных масштабах
- [ ] Производительность не ухудшилась

---

### Update 2: checkMouseMovement.m

**File:** `src/helpers/checkMouseMovement.m`

**Status:** 📋 Planned

**Changes:**
- Унифицировать код для Windows и macOS (строки ~78-121)
- Использовать новые функции вместо специфичного кода
- Сохранить fallback механизмы

**Before:**
```matlab
% Разный код для Windows и macOS
if ismac
    % macOS-specific code
else
    % Windows-specific code
end
```

**After:**
```matlab
% Унифицированный подход для обеих ОС
pixelCoords = getMousePixelPosition(app);

if ~isempty(pixelCoords)
    % Преобразовать пиксели в координаты данных
    currentPos = pixelToDataCoordinates(app, pixelCoords);
else
    % Fallback: использовать CurrentPoint
    if isprop(app, 'axPlot') && isvalid(app.axPlot)
        cp = app.axPlot.CurrentPoint;
        currentPos = cp(1, 1:2);
    else
        return;
    end
end
```

**Test Cases:**
- [ ] Работает на Windows
- [ ] Работает на macOS
- [ ] Унифицированный подход работает корректно
- [ ] Fallback работает при ошибках

---

## Testing & Verification

### Test Strategy
1. **Unit Tests:** Тестирование каждой новой функции отдельно
2. **Integration Tests:** Тестирование обновленных функций вместе
3. **Performance Tests:** Проверка производительности
4. **Cross-Platform Tests:** Проверка на Windows и macOS
5. **Edge Case Tests:** Проверка граничных случаев

### Unit Tests

**Test 1: pixelToDataCoordinates accuracy**
```matlab
function test_pixelToDataCoordinates_accuracy()
    % Arrange
    app = TableGraphEditor;
    app.axPlot.XLim = [0, 1000];
    app.axPlot.YLim = [0, 1];
    % ... setup axes size
    
    % Act
    pixelCoords = [500, 200]; % Пиксели относительно axes
    dataCoords = pixelToDataCoordinates(app, pixelCoords);
    
    % Assert
    % Проверить, что координаты соответствуют ожидаемым
    assert(abs(dataCoords(1) - 500) < 1, 'X координата неточна');
    assert(abs(dataCoords(2) - 0.5) < 0.01, 'Y координата неточна');
end
```

**Status:** 📋 Planned

### Integration Tests

**Test 2: dragPoint with new functions**
```matlab
function test_dragPoint_with_new_functions()
    % Arrange
    app = TableGraphEditor;
    % ... setup test data with different scales
    
    % Act
    % Начать перетаскивание и переместить мышь
    % ... simulate drag
    
    % Assert
    % Проверить, что координаты обновились точно
    % Проверить, что данные в таблице обновились корректно
end
```

**Status:** 📋 Planned

### Performance Tests

**Test 3: Performance comparison**
```matlab
function test_performance_comparison()
    % Arrange
    app = TableGraphEditor;
    % ... setup test data
    
    % Act
    % Измерить время выполнения с CurrentPoint
    % Измерить время выполнения с новыми функциями
    
    % Assert
    % Проверить, что разница незначительна (< 10%)
end
```

**Status:** 📋 Planned

---

## Documentation Updates

### Files to Update
- [ ] Обновить комментарии в новых функциях
- [ ] Обновить `DRAG_AND_DROP_COORDINATES_ANALYSIS.md` с результатами реализации
- [ ] Обновить `CROSS_PLATFORM_ANALYSIS.md` (если требуется)
- [ ] Обновить `task_20260106_drag_and_drop.md` со ссылкой на завершенную задачу

---

## Progress Log

### 2026-01-10
- Task created
- Plan defined
- Dependencies identified

### Phase 1: In Progress
- [ ] Function `pixelToDataCoordinates.m` created
- [ ] Function `getMousePixelPosition.m` created
- [ ] Function `cacheAxesInfo.m` created (опционально)
- [ ] Unit tests written and passed

### Phase 2: Pending
- [ ] `dragPoint.m` updated
- [ ] `checkMouseMovement.m` updated
- [ ] `axPlotButtonDown` updated (если требуется)
- [ ] Integration tests passed

### Phase 3: Pending
- [ ] Testing with different scales completed
- [ ] Cross-platform testing completed
- [ ] Performance testing completed
- [ ] Edge cases tested

### Phase 4: Pending
- [ ] Code optimized
- [ ] Documentation updated
- [ ] Code review completed

---

## Completion Checklist

Before marking this task as complete:

- [ ] Все функции Phase 1 реализованы и протестированы
- [ ] Все обновления Phase 2 выполнены и протестированы
- [ ] Все тесты Phase 3 пройдены успешно
- [ ] Документация обновлена (Phase 4)
- [ ] Код соответствует MATLAB_STYLE_GUIDE
- [ ] Код соответствует FPF-Summary принципам
- [ ] Работает на Windows
- [ ] Работает на macOS
- [ ] Производительность соответствует требованиям

---

## Sign-off

**Created by:** System Engineer (AI Assistant)  
**Date:** 2026-01-10  
**Status:** 📋 Planned  
**Next Steps:** Начать Phase 1 - создание вспомогательных функций

---

## Related Documents

- `DRAG_AND_DROP_COORDINATES_ANALYSIS.md` - Детальный анализ проблемы
- `task_20260106_drag_and_drop.md` - Базовая реализация перетаскивания
- `CROSS_PLATFORM_ANALYSIS.md` - Анализ кроссплатформенности
- `MATLAB_STYLE_GUIDE.md` - Руководство по стилю кода
- `FPF-Summary.md` - First Principles Framework

---

## Notes

- **Приоритет:** High - влияет на точность основного функционала
- **Оценка времени:** 6-8 дней (4 фазы)
- **Риски:** 
  - Возможные проблемы производительности при частых преобразованиях (митигировано кэшированием)
  - Совместимость с существующим кодом (митигировано fallback механизмами)
- **Зависимости:** Требует завершения `task_20260106_drag_and_drop.md`

